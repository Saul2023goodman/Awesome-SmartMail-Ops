# v1.36 业务范围与附件义务

## 业务判断

- 批量导入后的默认意图是创建全部通过核验的草稿，因此“选择”不是独立主任务。
- 搜索与联系状态只服务于例外：分批、重做、查找、排除；默认折叠为“筛选与排除”。
- 每行复选框仍保留，供用户排除少量邮件；联系策略拦截仍优先于默认纳入。

## 附件判断

附件待办必须来自可执行的发送义务，而不是材料中出现的任意链接。

1. 结构化附件字段中的文件名或材料名可进入匹配。
2. 普通网页、导师主页、论文页、来源链接属于研究证据，不生成附件待办。
3. 直接指向 PDF、Word、Excel、PowerPoint 或压缩包的链接仍保留，因为它可能是真实文件要求。
4. 被过滤的值保存在 `ignoredAttachmentRefs`，便于诊断，但不阻塞创建。

## 举一反三

- 院校字段只影响排程分组，不应因为识别不足阻断正文草稿。
- 来源、解析证据和内部说明属于可追溯信息，不应升级为用户待办。
- 只有会改变最终草稿正确性的未满足义务才进入阻塞态；其余不确定信息降级为提示或后台证据。
