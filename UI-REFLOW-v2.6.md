# UI Reflow v2.6 · Review-first Mail Audit

## 1. 重新确认这一步的业务目标

邮件审阅不是写邮件，也不是让用户重新编辑系统已经解析出的内容。它位于“导入/解析”和“排期/创建草稿”之间，真正目标只有一个：

> 用最少人工注意力确认自动解析有没有把一封完整邮件识别错。

因此用户最需要发现的错误不是普通文字修改，而是高风险结构错误：收件人串错、主题缺失、正文起点错、结尾/署名截断、后一封或附加说明侵入正文、重复邮件需要取舍。

## 2. 主界面：邮件块只承担扫描和定位

邮件状态板采用固定高度卡片。每张卡片只回答：

1. 这是谁的邮件？
2. 自动识别状态是什么？
3. 如果异常，异常是什么？
4. 我要不要点进去审阅？

正文摘要不再在卡片内展开，因为“预览 → 再点处理”制造了没有业务价值的第二层。点击卡片直接进入完整审阅。

状态采用颜色 + 形状 + 文案共同编码：绿色圆形=自动通过；蓝色圆角方形=已确认；橙色菱形=需处理；紫色六边形=冲突/重复。

## 3. 卡片防碰撞设计

- 4/3/2/1 列响应式网格；
- 144px 固定行高，卡片高度与网格行一致；
- 14px 横向 gutter、12px 纵向 gutter；
- hover 只改变边框与阴影，不做位移；
- 文本均采用单行截断；问题标签最多显示 2 个，其余折叠为 `+N`；
- `contain: layout paint` + `isolation:isolate` 避免阴影/内部布局侵入邻卡。

## 4. 弹窗：先审阅，不编辑

点击卡片后默认进入只读审阅模式。顺序按风险而不是字段表单排序：

### 4.1 核验状态

收件人 / 主题 / 开头 / 结尾 / 附件五个状态同时出现。它们用于告诉用户哪里值得先看，而不是展示解析技术细节。

### 4.2 开头核验

抽取称呼和首段。这里最容易发现：邮件起点错、收件人与称呼不一致、上一段说明侵入。

### 4.3 结尾核验

抽取收尾和署名。这里最容易发现：邮件终点错、署名被截断、下一封邮件或备注进入正文。

### 4.4 完整邮件

在同一弹窗继续从头到尾显示完整正文。用户无需切换页面，也无需进入编辑器才能确认中间内容。

## 5. 修正是次级流程

只有用户明确点击“发现问题，修正”，才出现收件人、主题、正文输入框。修正后可以“返回审阅”，重新按照开头/结尾/完整邮件进行一次确认。

这避免把每次人工审阅都变成“我是不是应该改点什么”的编辑任务。

## 6. 确认规则

- 缺收件人 / 缺主题 / 缺正文：属于确定性缺失，禁止直接确认，必须修正。
- 称呼 / 落款 / 邮件边界 / 低置信度：如果核心字段完整，用户阅读完整邮件后可以人工确认。
- 重复组：始终进入专门的版本取舍视图，不能被普通确认绕过。
- 单封确认后回到状态板；“确认无误，下一封”只前往下一封仍待人工处理的邮件。

## 7. QA 结果

自动 QA 使用真实导入流程，并额外克隆 12 张真实卡片 DOM 做压力布局。1366×611 与 820×700 下：

- 卡片重叠：0；
- P0：0；
- P1：0；
- 纵向嵌套滚动：0；
- 单封审阅弹窗越界：0；
- 次级修正模式越界：0。
