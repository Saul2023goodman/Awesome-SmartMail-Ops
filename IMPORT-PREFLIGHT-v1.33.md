# v1.33 · Import Preflight

## 问题根因

v1.32 按“当前最高优先级”压缩界面时，把附件入口过度后置。业务上附件虽然通常晚于收件人/重复判断处理，但它仍是用户在导入阶段需要提前准备的真实对象。结果是：**优先级正确了，可发现性却丢了**。

同一屏里也存在另一个结构矛盾：邮件、参考总名单、附件都被放进“添加资料”，让用户误以为三者是同一种来源；实际它们承担完全不同的职责：

- **邮件资料**：生成草稿任务，是必需主输入。
- **参考总名单**：增强联系人核对和院校上下文，可选。
- **附件资料**：为邮件要求提供文件，可能成为后续硬阻塞；即使当前未识别出要求，也应保留明确入口。

## 新业务模型

上传阶段改成两段式：

1. **导入邮件资料**
2. **补充本批次资料（Preflight）**

邮件导入成功后立即弹出一个批次准备对话框，把“参考总名单”和“附件资料”并列展示。用户可以现在补充，也可以明确跳过；未补充的项目之后仍有固定入口。

### 参考总名单 Box

- 标注“可选增强”。
- 上传后显示联系人条数。
- 不上传不会影响当前批次基础查重。
- 以后仍可从“批次准备”重新补充。

### 附件 Box

- 永久存在，不依赖当前是否已经检测到附件需求。
- 如果邮件中已有附件要求，直接显示需求数量与前几个文件名。
- 支持一次选择附件文件夹或单独选择文件。
- 现在不添加并不会删除附件需求；未匹配项继续进入步骤 2 待办。

## 为什么使用导入完成弹窗

它解决的是“时机”而不是“强调颜色”：

- 导入前，系统还不知道哪些附件真正需要，提示过早。
- 进入处理待办以后再提示，总名单和附件准备又太晚，容易返工。
- **刚完成邮件解析**时，系统第一次同时知道“有多少封邮件、有哪些附件要求、是否已有参考名单”，因此这是信息最完整且成本最低的准备节点。

弹窗只出现一次，但不是一次性入口。关闭后，上传区保留“批次准备”状态条：

- 参考总名单：待确认 / 未添加 / N 条已加入
- 附件：待确认 / 暂未添加 / N 个文件 / N 项待补

用户可以随时点击“补充资料”重新打开。

## 上传页 UI 重构

### 空状态

上传页只表达一个主任务：**导入邮件资料**。

三个入口分别是：文件、文件夹、粘贴内容。说明文案明确告诉用户：总名单与附件会在导入完成后集中准备，不再要求用户理解“混合来源分流”。

### 已导入状态

导入动作降级成维护工具栏：

- 添加文件
- 添加文件夹
- 粘贴补充

其下增加稳定的“批次准备”状态条。系统读取细节继续放在“导入详情”折叠区。

## 流程语义调整

流程 1 的完成条件从“已经选择过文件”调整为：

> 邮件已导入，并完成一次批次准备决策。

因此右侧流程显示：

- 1 添加资料：邮件 → 批次准备
- 2 处理待办：内容 · 去重 · 附件
- 3 选择与安排
- 4 创建草稿

如果用户在 Preflight 里暂不添加附件，而邮件确实要求附件，步骤 1 仍可完成；附件以明确 blocker 进入步骤 2。这样“准备入口”与“真正处理阻塞”职责分开。

## 举一反三

这次形成三个可复用规则：

1. **低优先级不等于可以隐藏**：未来会影响任务完成的对象，应在最合适节点可见，但不必成为当前主 CTA。
2. **不同业务角色不要共用一个泛化“上传资料”入口**：主输入、增强上下文、执行资源应在信息架构上分层。
3. **一次性提示必须配永久入口**：弹窗负责在正确时机建立认知，状态条负责之后的可恢复和可发现性。

这套模型可继续用于历史联系记录、CV 多版本、推荐信、作品集、模板等“导入后才知道是否需要，但之后又不能消失”的辅助资源。

## 验证

- 18 组 JavaScript 回归测试通过。
- Context Preflight、Task Hierarchy、完整复杂用户旅程 Playwright 测试通过。
- 复杂样本中导入 6 封邮件后，Preflight 正确显示 3 项附件需求：`CV-Alice.pdf`、`CV-Erin.pdf`、`Transcript-Erin.pdf`。
- 完成批次准备后，步骤 2 正确显示 3 个附件待补；重复邮件排除后实时重算为 2 个。
- 补齐附件后自动进入选择与安排。
- Chromium 页面级 JavaScript error = 0。
