# v1.46 · Decision-first Routing

## 业务问题

上传后的核验步骤只需要解决一个问题：**系统给这个文件分的用途对不对？** v1.45 中间列仍直接拼接表头和内容，信息量大但决策价值低；同时，部分 Excel 文件文件名已经明确包含“补充名单 / 导师名单”，主表结构也明显是导师表，却仍可能因为辅助工作表或弱证据进入“待确认”。

## UI 调整

- 中间列只保留快速核验摘要，不再倾倒原始字段。
- 邮件：收件人 + 主题。
- 总名单：记录数 + 最多三个关键字段 + 首条数据示例。
- 附件：短内容摘要。
- 待确认：直接说明需要用户判断的原因。
- 新增轻量列标题“文件 / 快速核验 / 用途”，右侧仍承担完整内容阅读和用途修改。

## 识别策略

名单识别采用双证据原则：

1. **文件名意图**：总名单、补充名单、导师名单、教授名单、联系人名单、套磁名单等。
2. **内容结构**：学校 / 导师 / 邮箱等身份字段，以及导师链接 / 研究方向 / 联系状态等辅助字段。

文件名不会单独决定分类：

- `导师名单.xlsx` + `导师 / 研究方向` + 多条记录 → 高置信总名单。
- `北京985211补充名单.xlsx` + `学校 / 导师 / 导师链接 / 研究方向` → 高置信总名单。
- `名单.xlsx` + `设备 / 数量 / 价格` → 待确认。
- `附件清单.xlsx` → 不应用名单文件名加权。
- `发送清单.xlsx` 若具有收件人 / 主题 / 正文 → 邮件结构优先。

## 多工作表

同一源文件若一个主工作表具有 >=85 的明确用途，而其他工作表只是低置信辅助/空白结构，则以主用途作为文件级结果；只有出现另一个 >=70 且接近的竞争用途时才继续进入待确认。
