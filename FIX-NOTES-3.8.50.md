# SmartMail Ops v3.8.50

## 颜色近似度优化

- 颜色不再按 HEX 完全相等拆组，改为基于 CIELAB 感知距离的近似色聚类。
- 同一行内先合并相近填充色，再计算 dominant color，避免人工表格中的轻微色差把一个主色拆成多个竞争色。
- 跨行再次按近似度合并颜色族；点击一个“近似色”特征会一次选中该颜色族的全部联系人。
- 每个颜色族保留出现最多的原始色作为代表色，不改写 Excel 原始样式。
- 颜色族按感知近似度相邻排列，方便快速识别同系颜色。
- 近似色仅作为人工选人的辅助特征，仍不会自动创建 R1/R2 或自动分批。
- 严格显式批次识别、合并单元格视觉保持、dominant-row 逻辑继续保留。

## 阈值

- 默认 CIELAB ΔE76 <= 12 视为同一颜色族。
- 该阈值用于吸收人工填色/复制粘贴造成的轻微色差，同时避免把明显不同色系直接合并。
