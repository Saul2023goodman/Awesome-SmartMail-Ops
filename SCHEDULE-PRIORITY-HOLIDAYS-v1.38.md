# v1.38 名单优先级与节假日排程

## 新增能力

- **同校按名单顺序排队**：总名单中的 `套磁顺序 / 联系顺序 / 发送顺序 / 优先级 / Priority / Rank / Order / Sequence` 会被识别为排程优先级。数字越小越优先，只改变同一院校内部的先后，不打乱不同院校的独立轮次。
- **读取国家字段**：支持 `Country / 国家 / 国家地区 / 国家/地区 / 所在国家`，用于选择公共假日规则。
- **跳过节假日（含周末）**：自动排程默认开启。某一轮的目标日期如落在周末或已支持国家的公共假日，会只把该封邮件顺延到下一个工作日；下一轮仍从原始轮次锚点计算，不发生“越顺延越漂移”。
- **保留已有时间不静默修改**：勾选“保留已有时间”时，手工/导入时间保持不变；若它落在周末或公共假日，面板会提示休息日冲突，而不是偷偷改时间。

## 当前内置国家公共假日

根据总名单 `Country` 字段识别：

- United States / 美国：联邦公共假日（含 observed day）。
- Canada / 加拿大：主要全国/联邦公共假日。
- Australia / 澳大利亚：全国层面的公共假日；州级差异假日不猜测。
- United Kingdom / 英国：英格兰/威尔士通用 bank holidays。
- New Zealand / 新西兰：主要全国公共假日（不硬编码年度变化较大的 Matariki）。

未覆盖国家仍会避开周末，并在应用排程后的状态信息中明确提示“仅避开周末”。

## 排序安全规则

1. 只有同一学校分组内会比较优先级；不同学校不会互相抢占轮次。
2. 有数字顺序的记录优先于无顺序记录；相同顺序保持原导入次序，保证稳定排序。
3. 已被“保留已有时间”保护的任务不强行按优先级重排；如果希望全部按名单顺序重排，需要取消“保留已有时间”。
4. 节假日顺延发生在轮次分配之后，因此不会破坏“同校每轮最多 N 位”的业务约束。

## 针对本次 48 位导师名单

该表的 `Country / University / Email / 套磁顺序` 可以直接被识别。比如同为 Australian National University 的导师会按 `套磁顺序` 数字从小到大安排，而不是按 Excel 原始行顺序。
