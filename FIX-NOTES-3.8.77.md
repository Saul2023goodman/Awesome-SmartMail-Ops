# Follow-up monitor usability

- Make the current actionable Follow-up total a clickable summary control.
- Clicking the summary switches directly to the actionable filter and clears search.
- Replace technical sequence-first labels such as `Follow-up #1 到期` with task-oriented Chinese copy such as `现在可以跟进`; sequence is secondary context.
- Rename monitor bulk actions to user-oriented wording (`选择当前可跟进`, `批量准备跟进`).
- Clarify scheduled, waiting, completed, and current-step labels.
