# v2.2 决策式工作流重构

## 本轮判断

问题不是某一个滚动条、某一个弹窗或某一组控件尺寸失控，而是旧界面仍按“功能组件”堆叠：上传、分类、名单、附件、待办、查重、排期都能在相邻区域同时出现。结果是窗口越窄，组件只能继续缩小、嵌套滚动或互相争抢空间。

v2.2 改为一个更严格的规则：**一个工作面只服务一个用户决定。** 数据可以共享，业务逻辑可以复用，但界面不能跨阶段同时出现。

## 三个核心原因与举一反三

1. **阶段状态和 DOM 可见性没有成为硬约束。** 过去渲染函数只要发现有数据，就可能把卡片重新显示出来，所以“第 1 步”仍能出现待办与联系人核验。v2.2 把阶段可见性提升为最高优先级：导入、待办、排期、创建互斥显示。以后任何新增功能都先归属一个阶段，再决定是否渲染。
2. **复杂度不同的任务被按同一网格平均分配。** 总名单是一次性轻操作，附件却包含文件库、用途、缺失项和修改动作；把二者塞进同一窄侧栏必然失衡。v2.2 先进入“批次资料”，再在“参考名单 / 附件”之间切换，附件获得完整工作面。以后不再按“两个功能各占 50%”布局，而按操作复杂度分配空间。
3. **响应式曾以压缩为主，而不是改变交互模型。** 排期曾在窄窗口继续保留规则与邮件表格并排，最终只能缩字体、缩控件并增加滚动。v2.2 在所有窗口都拆成“排期规则 / 邮件时间”两个完整工作面；文件核验在窄窗口使用列表 → 详情钻取。以后窗口变窄时优先减少同时出现的信息，而不是继续缩小 UI。

## 新业务视图

批量主流程仍保持原逻辑：

`导入资料 → 核验待办 → 选择与排期 → 创建草稿`

但每一步内部进一步按用户动作拆开：

- 导入资料：只显示本批次资料入口，不混入待办或联系人核验。
- 导入结果：`核验文件 / 批次资料`。
- 批次资料：`参考名单 / 附件`，两者不再共享窄侧栏。
- 选择与排期：`排期规则 / 邮件时间`，不再左右挤压。
- 创建草稿：只显示最终选择与创建动作。

## 滚动所有权

页面、主阶段容器、弹窗外壳都不承担业务纵向滚动。只有真正需要浏览大量同类内容的区域可以滚动，例如文件列表、文件详情正文、附件列表、邮件列表和联系人表格。目录导航可独立滚动，但不会与整个侧栏形成第二层滚动。

## 文案原则

主界面只回答“现在要做什么、当前结果是什么、下一步是什么”。诸如分类算法、置信度、自动分流、回退策略、增强参考来源等实现机制不再作为用户说明出现。自动能力仍保留，但用结果状态表达，而不是解释内部机制。

## 核验

`qa/ui_audit.py` 现在额外覆盖：

- 导入后返回上传页，确认第 1 步不出现第 2 步模块且没有外层滚动；
- 批次资料的参考名单与附件两个独立工作面；
- 排期规则与邮件时间两个独立工作面；
- 1440×900 到 820×700 的文件核验；
- 单封草稿、联系人、创建页的桌面与窄窗口。

当前自动布局审计 P0 = 0，P1 = 0。
