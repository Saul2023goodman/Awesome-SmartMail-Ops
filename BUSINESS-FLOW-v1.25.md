# v1.25 业务流程：把“能力”与“可选资料源”拆开

## 1. 这次暴露的根本问题

旧流程里，“查重”事实上挂在“总名单交叉核验”下面：只有总名单存在时，`crossCheck()` 才会发现“同一名单联系人对应多封当前邮件”。

这会制造一个错误的用户心智：

> 好像必须先导入总名单，系统才有资格判断当前批次有没有重复。

但“当前批次是否把同一个收件人放进了两次”只依赖当前批次本身，与总名单无关。因此旧结构把一个**基础能力**错误绑定到了一个**可选数据源**。

## 2. 新的三层核验模型

### A. 本批次自检：永远执行

只要邮件任务已经生成，就自动执行：

- 必填内容检查：收件人、主题、正文；
- 解析歧义检查；
- 当前批次查重；
- 邮件要求的附件是否已找到。

查重证据分两级：

1. **同一邮箱**：确定性高，直接作为重复组；
2. **同名 + 同一可信院校**：邮箱缺失时作为疑似重复组。

两类都会进入“检查邮件”，由用户确认或修改后继续。仅“同名”不足以判重，以避免不同学校同名导师被误合并。

### B. 邮箱历史：有记录就增强，不作为前置条件

联系人库已经有邮箱同步信息时，联系人核验面板额外展示：

- 之前是否发送过；
- 是否已有草稿；
- 最近主题等历史线索。

这些信息默认**只提示，不阻塞**。原因是“以前联系过”可能代表正常 follow-up，不能自动等同于错误重复。

### C. 参考总名单：可选增强

有总名单时，再增加：

- 当前邮件是否在名单中；
- 名单中的联系人是否还未加入当前批次；
- 一个邮件对象是否对应多个名单候选；
- 缺少院校时是否可以补全；
- 缺少邮箱时是否能给出可靠候选；
- 院校差异如何影响自动错峰。

没有总名单时，上述增强能力缺席，但**基础查重、解析、附件检查和草稿流程完全可运行**。

## 3. 新的主流程

用户视角只保留四步：

1. **添加资料**
   - 邮件资料：主数据，必须有；
   - 附件：只有邮件明确要求时才需要；
   - 参考总名单：可选；
   - 其他 CV / 研究计划 / 普通文档：先分流，不进入邮件任务。

2. **检查邮件**
   - 系统自动做当前批次查重；
   - 自动检查缺字段、解析歧义和附件；
   - 邮箱历史与总名单作为增强证据合并展示；
   - 确定性缺项补齐后自动消失；
   - 真正需要判断的重复/歧义才要求用户确认。

3. **选择与安排**
   - 只对已经通过必要核验的邮件开放；
   - 用户决定本次创建哪些草稿；
   - 院校只作为自动错峰的后台证据；
   - 没有院校时使用保守错峰，不要求用户补技术字段。

4. **创建草稿**
   - 只创建已选择且状态 ready 的邮件；
   - 运行过程中一旦单封出现页面级失败，立即停止，避免串稿；
   - 永不自动发送。

## 4. 为什么“联系人核验”要合并展示

旧界面容易把“总名单差异”误解成唯一的联系人安全检查入口。

新版统一为“联系人核验”，内部可以来自三种证据：

- 当前批次；
- 邮箱历史；
- 参考总名单。

用户只需要回答：

> 这些邮件有没有重复对象、已有联系记录或名单差异？

而不是先理解“这条结论到底来自 roster、contacts 还是 parser”。

## 5. 重复确认为什么不能只是一个布尔值

如果记录成 `duplicateConfirmed=true`，会有一个隐藏风险：

1. 用户确认过 A/B 两封重复；
2. 后来又加入 C，仍是同一邮箱；
3. 旧的 `true` 可能让系统误以为新的三封关系已经确认。

因此 v1.25 将确认绑定到稳定的**重复组 ID**：

- `email:xxx@example.edu`
- `name-school:<normalized identity>`

修改收件人或院校会清掉相应旧确认；新增重复任务至少会让新任务重新进入核验，避免旧状态吞掉新风险。

## 6. 举一反三：以后所有业务依赖都按同一原则处理

### 原则一：基础能力不能依赖可选资料

错误例子：

- 查重必须有总名单；
- 排程必须识别出学校；
- 导入必须先同步联系人；
- 附件区必须先手工选择文件才能继续。

正确做法：

- 当前批次自己能完成的判断先完成；
- 可选资料只提高覆盖率、精度或便利性。

### 原则二：只有真实业务风险才阻塞

阻塞：

- 缺少收件人/主题/正文；
- 当前批次重复但尚未确认；
- 真正的联系人匹配歧义；
- 邮件明确要求附件但附件缺失；
- 无法执行的定时值。

只提示：

- 以前发送过；
- 已经有旧草稿；
- 总名单院校与来源院校不同，但排程可采用更可信值；
- 总名单中还有尚未加入本批次的人。

### 原则三：用户修好确定性问题后自动推进

补上主题、补上合法邮箱、添加缺失附件后，不再要求“再点一次确认”来证明系统已经看到这个事实。

只有重复、解析歧义、人工大幅修改等真正需要人作判断的情况保留显式确认。

### 原则四：展示业务结论，不展示内部模块

用户看到：

- “当前批次重复 2 组”；
- “3 封已有邮箱记录”；
- “参考名单还有 5 位未加入”。

不需要看到：

- roster index；
- contact snapshot；
- parser confidence pipeline；
- scheduler institution key。

## 7. P0 风险边界

本次特别避免两类 P0：

1. **漏报重复**：无总名单时同一邮箱重复现在仍会被拦截；
2. **旧确认误放行新关系**：重复确认按组保存，身份改变会重新核验。

同时避免反向 P0：不因为“历史上联系过”就强制阻塞正常 follow-up，不因为仅同名就把不同导师合并。
