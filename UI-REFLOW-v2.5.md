# UI Reflow v2.5 · Mail Status Board

## 1. 本轮真正要解决的问题

v2.4 已经把“邮件审阅”从流程阶段中解耦出来，但实现仍然是一个几乎覆盖整个工作区的审阅弹窗：左侧队列、右侧编辑器，打开后用户必须进入“逐封邮件模式”。

这与批量邮件场景的真实任务不一致。大多数邮件已经被系统正确解析，用户真正要做的是：

1. 快速确认整批邮件的自动识别状态；
2. 一眼找到少数异常邮件；
3. 只对异常邮件投入人工注意力；
4. 必要时抽查正常邮件，而不是逐封确认。

因此 v2.5 将邮件审阅从“队列 + 大编辑器”重构为 **邮件状态板 + 单封处理弹窗**。

---

## 2. 新的核心原则：对象先于编辑器

邮件审阅主界面不再围绕编辑器组织，而是围绕“每封邮件本身”组织。

每封邮件都以一个独立邮件块显示。邮件块承担三件事：

- 表示这是谁、发给谁、主题是什么；
- 直接表达自动识别状态；
- 告诉用户是否需要行动。

编辑器不再永久占据页面。只有以下情况才打开单封弹窗：

- 邮件存在必须人工处理的问题；
- 用户主动选择编辑一封已自动通过的邮件。

这意味着 UI 的默认状态是“浏览”，不是“编辑”。

---

## 3. 状态视觉编码

v2.5 不再只依赖文字“可直接使用 / 待处理”。状态同时使用颜色、形状和文案编码，避免用户逐行阅读。

### 自动通过

- 绿色左侧状态线；
- 绿色圆形 ✓；
- 文案“自动通过”；
- 表示收件人、主题、正文已满足创建条件。

### 已人工确认

- 蓝色状态线；
- 蓝色圆角方形 ✓；
- 文案“已人工确认”；
- 与机器自动通过区分，保留人工决策痕迹。

### 需要人工处理

- 橙色状态线；
- 菱形 !；
- 卡片顶部使用暖色底；
- 直接出现“处理此封”按钮；
- 缺收件人、主题、正文或低确定性问题都进入这一类。

### 重复 / 需要取舍

- 紫色状态线；
- 六边形 ◆；
- 文案“需要取舍”；
- 使用独立“处理取舍”动作，避免被普通批量确认误处理。

颜色不是唯一信息载体，形状和文字同时存在。

---

## 4. 邮件块的两级浏览

### 默认折叠

主界面只显示：

- 状态；
- 邮件对象；
- 收件人；
- 主题；
- 状态摘要。

这样一屏可以扫描更多邮件。

### 原地展开

点击邮件块本身，只在卡片内部展开正文摘要和问题标签，不进入弹窗。

因此“我只是想看一下内容”和“我要处理这封邮件”被拆成两个动作。

这避免了 v2.4 中点击任何邮件都会把用户带入大型审阅工作台的问题。

---

## 5. 单封处理弹窗

真正需要编辑时，才进入单封邮件弹窗。

弹窗只包含当前邮件：

- 当前问题提示；
- 收件人；
- 主题；
- 正文；
- 重复邮件取舍；
- 排除此封；
- 确认 / 确认并处理下一封。

批次队列不再出现在弹窗中。用户关闭弹窗后仍留在原来的邮件状态板位置。

“确认并处理下一封”优先跳到下一封仍需人工处理的邮件，而不是顺序打开一封已经自动通过的邮件。

---

## 6. 筛选和搜索不再触发弹窗

旧逻辑中，切换筛选或搜索后，如果当前邮件不在结果中，界面会自动打开搜索结果中的第一封邮件。这会造成明显的被动跳转。

v2.5 改为：

- 搜索只改变邮件块集合；
- “全部邮件 / 只看需处理”只改变可见范围；
- 不自动进入单封编辑；
- 只有用户明确点击“处理 / 编辑”才打开弹窗。

这是本轮“用户主动发起深度操作”的核心约束。

---

## 7. 举一反三后的业务规则

### 规则 A：自动化结果应该默认可扫视，而不是默认要求人工确认

系统已经有识别能力，就应把人工精力集中在不确定项，而不是把自动化结果重新变成一份人工表单。

### 规则 B：异常要通过视觉差异主动暴露

异常不能只靠一行小字。颜色、形状、状态线、动作按钮共同承担提醒责任。

### 规则 C：浏览与编辑必须分层

高频浏览留在主页面，低频编辑进入弹窗。不能因为“用户可能会编辑”就长期显示编辑器。

### 规则 D：筛选、搜索、排序不能偷偷改变任务上下文

这些操作只改变“我看到什么”，不应自动替用户决定“我现在要编辑什么”。

### 规则 E：下一步应该沿着未完成事项推进

“确认并下一封”应寻找下一封异常邮件，而不是机械按列表顺序前进。

### 规则 F：状态编码必须可冗余识别

不能只用颜色。颜色 + 形状 + 文案同时存在，兼顾快速识别和可访问性。

---

## 8. 响应式策略

### 宽屏

邮件块自动形成多列网格，状态、搜索、筛选与图例同屏。

### 820×700 等紧凑窗口

- 邮件块自动降为更少列；
- 图例在非常窄的窗口可隐藏，但每个邮件块自身仍保留状态形状和状态文字；
- 主页面继续由邮件块区域承担滚动；
- 单封弹窗使用接近全屏但保留边距的任务窗口；
- 不新增页面外层滚动。

---

## 9. 代码变化

### `app.js`

- 邮件审阅 DOM 改为状态板结构；
- `reviewVisualState()` 统一生成邮件块视觉状态；
- `renderReviewQueue()` 改为邮件块渲染；
- 邮件块支持原地正文摘要展开；
- 单封编辑器变为真正的局部 modal；
- 审阅入口不再自动打开第一封邮件；
- 搜索 / 筛选不再自动打开邮件；
- 批量确认选中态适配邮件块；
- “确认并下一封”只继续处理待办邮件；
- `syncModalState()` 只把单封编辑器视作 modal，不再把整个审阅页视作 modal。

### `workflow-dialogs.css`

- 取消 v2.4 审阅页的 fixed portal 外观；
- 将审阅页明确放入应用主工作区 grid；
- 新增邮件状态块、状态形状、颜色和展开态；
- 新增单封处理弹窗；
- 新增 820×700 与低高度窗口适配。

### `qa/ui_audit.py`

- 新增邮件状态板截图；
- 新增 1366×611 / 820×700 单封处理弹窗截图；
- 断言进入审阅后默认不打开编辑器；
- 继续检查 P0/P1、控件越界与嵌套滚动。

---

## 10. 验证结果

真实 Word + Excel + PDF 导入流程通过。

自动 UI QA：

- 静态 / 架构检查：PASS；
- 独立工作台加载：PASS；
- 真实导入：PASS；
- 阶段导航与隔离：PASS；
- 1366×611 邮件状态板：P0 = 0 / P1 = 0；
- 820×700 邮件状态板：P0 = 0 / P1 = 0；
- 1366×611 单封弹窗：P0 = 0 / P1 = 0；
- 820×700 单封弹窗：P0 = 0 / P1 = 0。
