# QA Harness · NetEase Mail Draft Assistant v1.47.0

这是一个仅供开发核验的隔离预览入口，不替换生产扩展，也不修改正式 `manifest.json`。

## 作用

- 直接加载本目录中的真实 `content.js`、`content.css`、解析器、排程器、联系人与名单模块。
- 用本地模拟网易邮箱页面提供写信入口、Compose DOM、正文 iframe、附件控件和“存草稿”成功反馈。
- 可核验工作台打开、单封草稿、批量资料导入、来源分类预览、定时字段和保存成功边界。
- 不登录网易邮箱，不读取真实草稿箱，不发送邮件。

## 运行

1. 用 Chromium / Chrome 打开 `qa-harness.html`。
2. 点击右下角 N 图标打开工作台。
3. 在“单封草稿”中可验证 Compose 填充和本地保存提示。
4. 在“批量草稿”中选择 `qa-fixtures/` 下的 CSV 文件，验证导入和上传后文件用途核验。
5. 截图时固定浏览器窗口尺寸，建议先记录 1440×900，再补充窄窗口视图。

## 测试夹具

- `qa-mail-sample.csv`：一封包含主题、正文、学校和定时字段的邮件。
- `qa-roster-list.csv`：联系人/导师总名单结构。
- `qa-attachment-checklist.csv`：用于验证附件清单不会被误判为总名单。

## 生产扩展

生产路径仍以正式 `manifest.json` 为准：在 Chrome / Edge 的扩展管理页开启开发者模式，加载本目录作为已解压扩展，再在 `https://mail.163.com/` 中使用。生产扩展只创建/保存草稿，不自动发送。

## 核验底线

每篇批量邮件必须在点击“存草稿”后检测到新的成功证据，确认后才能进入下一篇；保存失败应立即停止。harness 的成功提示只模拟这一业务边界，不代表真实网易账号状态。
