# v1.14 业务工作台重构说明

## 1. 重新定义业务对象

用户真正管理的不是“导入模块、排程模块、核对模块”，而是一批尚未变成安全草稿的外联邮件。

因此界面需要始终回答五个问题：

1. 资料是否已经进入系统？
2. 哪些邮件还没有通过质量门？
3. 这次真正要创建哪些邮件？
4. 哪些邮件需要安排时间？
5. 哪些草稿已经安全保存？

## 2. 为什么“逐条确认”必须独立

逐条确认承担的是 **quality gate**，而不是“导入失败后的补救”。它同时服务两类行为：

- 被动处理：机器识别后仍存在缺失、冲突、歧义的邮件。
- 主动核验：用户希望抽查自动通过的邮件。

如果把它埋在导入区，会产生三个问题：

- 用户容易认为导入结束就等于核验结束。
- 核验队列、证据、编辑和批量修正挤压导入区，职责混乱。
- 后续返回检查时需要重新找到导入页面中的深层位置。

因此一级导航改为：**批量工作台 / 逐条确认 / 单封草稿 / 联系人**。逐条确认拥有待处理计数、筛选、队列、证据、编辑和批量处理能力。

## 3. 为什么批量补主题放进逐条确认

“补主题”本质上是内容质量修正，不属于导入，也不属于草稿执行。

它被放入逐条确认，并改为选择驱动：

- 用户先勾选邮件；
- 批量动作栏才出现；
- “批量补主题”仅处理所选项目中的空白主题；
- 已有主题绝不覆盖；
- 修正后可直接“保存并确认所选”。

这样将“发现重复问题 → 批量修正 → 批量放行”放在一个工作区里，减少逐封打开、保存、关闭的机械操作。

## 4. 为什么流程改成右侧固定竖列

批量工作台属于长页面：准备资料、任务列表、排程和执行区天然存在纵向滚动。

顶部横向流程条在用户下滚后会消失，导致：

- 用户忘记当前阶段；
- 想跨阶段返回时需要滚回顶部；
- 横向空间不足时步骤文字被迫压缩；
- 流程指引本身反而增加滚动成本。

v1.14 将流程改为右侧 `position: sticky` 竖列：

- 在当前工作台滚动期间持续可见；
- 竖排更适合“步骤名 + 状态 + 辅助说明”的扫描；
- 主内容仍独立滚动，不会被全局 fixed 元素遮挡；
- 小屏自动压缩为只保留步骤编号的窄轨道。

这里刻意使用 `sticky` 而不是视口级 `fixed`：它实现用户感知上的“固定”，但仍受工作台容器约束，不覆盖网易邮箱主体或其他工作区。

## 5. 一次类推后的继续收敛

### 去掉第二套抽查入口
批量页不再展示独立“抽查邮件表”。待确认和主动抽查统一到逐条确认，避免两个页面编辑同一批邮件。

### 把“确认准备完成”改成“进入任务选择”
质量确认已经由独立质量门承担；这里再叫“确认”会产生重复心智模型。该按钮只负责把已通过质量门的批次推进到任务选择阶段。

### 批量动作按选择出现
没有选中任何邮件时，不展示补主题和批量确认操作；选中后才出现动作栏，降低常驻操作噪音。

### 正常路径与维护路径继续分离
联系人页的“同步邮箱”是日常动作；“重建联系人记录”属于异常维护，继续折叠，不与主操作竞争。

## 6. 参考设计原则

- Carbon Design System：多步骤任务应清楚显示完成、当前、未开始等状态；条件允许时进度指示器优先竖排；表格批量动作应围绕选中项出现。
- GOV.UK Design System：任务名称和状态应清晰，复杂任务应拆成用户能够理解的小任务；状态种类应保持最少、突出仍需处理的事项。
- Nielsen Norman Group 通用可用性原则：系统状态可见、符合用户真实世界语言、减少不必要信息、通过渐进披露控制复杂度。

## 7. 不改变的安全边界

- 只创建并确认保存草稿，不自动发送。
- 逐封确认网易“存草稿”结果后才继续。
- 批量执行发生错误立即停止，避免对象错配或串稿。
