# v1.13 业务工作台重构说明

## 1. 业务目标

这个插件的核心不是“管理导入、排程、联系人等功能”，而是帮助用户安全地完成两类任务：

- 批量：把一批外联资料变成可控、可核对、可定时的网易草稿。
- 单封：临时创建一封不属于批次的草稿。

联系人属于长期状态管理，不是一次批次的步骤。

## 2. 重构后的信息架构

### 批量工作台
准备邮件 → 必要核对 → 选择任务 → 安排时间 → 创建草稿

“导入与核对”和“批量创建”不再是两个一级模块；它们是同一批次的连续阶段。

### 单封草稿
只做一次性的单封创建，不承载批量配置。

### 联系人
只负责长期状态、跟进、邮箱同步和异常维护。

## 3. 设计原则如何落地

- **按用户任务组织，不按内部能力组织**：一级导航从 4 项减为 3 项；批量相关能力全部收进一条业务链。
- **减少重复**：批量流程指引只出现一次；导入完成后不再跨页面重复解释流程。
- **减少同义操作**：ZIP 直接归入“选择文件”；任务选择保留“选择筛选结果 / 取消全部选择”，隐藏低频的“取消当前结果”。
- **渐进披露**：字段映射、来源结构、联系人重建等异常能力默认折叠。
- **正常路径与维护路径分离**：联系人页主操作是“同步邮箱”；完整重建改为“维护选项 → 重建联系人记录”。
- **状态优先于技术细节**：默认界面围绕“准备是否完成、还有多少问题、选了多少任务、是否已排程、草稿是否成功”。

## 4. 参考设计理念

重构参考了以下通用原则：

- Nielsen Norman Group：Visibility of system status；Match between system and the real world；Consistency and standards；Aesthetic and minimalist design；Progressive disclosure。
- GOV.UK Service Manual：从用户完整目标出发、让用户做尽可能少的事情、不要把内部结构暴露给用户、按用户认识的任务划定 transaction。
- Carbon Design System：数据表批量动作应围绕当前选择出现；搜索、筛选、批量动作和表格职责应清晰分区。

## 5. 不改变的安全边界

- 只创建并确认保存草稿，不自动发送。
- 批量执行遇到错误立即停止，避免串稿。
- 附件、定时、联系人拦截和原有识别/校验逻辑保留。
