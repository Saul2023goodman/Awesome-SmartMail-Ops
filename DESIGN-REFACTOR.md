# v1.17 界面重构说明

## 核心流程

插件只围绕用户真正要完成的一件事组织：**把一批邮件整理成可以安全创建的草稿。**

**添加资料 → 检查邮件 → 选择与安排 → 创建草稿**

“解析预览”仍属于批量草稿内部；“一键填写主题”也不是独立模块，而是在用户填写主题之后按上下文出现的快捷建议。

## 检查邮件：从“三个小窗”变成完整阅读

旧布局把邮件列表、原文、编辑区三栏同时限制在 viewport 高度里，原文和编辑区各自滚动，正文 textarea 又再次滚动。邮件越长，用户越难判断自己是否看到完整内容。

v1.17 改为：

- 左侧邮件列表只负责导航，可以独立滚动；
- 右侧原文和邮件内容上下连续排列；
- 原文与编辑区不再设置 viewport `max-height`；
- 正文 textarea 自动随内容增高；
- 操作按钮不悬浮遮挡正文。

因此长邮件由工作台主区域统一滚动，而不是在多个嵌套小窗里滚动。

## 自动安排时间：默认展开

排程已经是“选择与安排”的核心动作，不属于高级设置。进入新批次的选择阶段后默认展开，让用户直接看到开始时间、同校上限、间隔与“保留已有时间”。

用户主动收起后，普通界面刷新不会强行重新展开；开始新的导入批次时再恢复默认展开。

## 主题：从批量工具变成下一步建议

旧设计需要先选中邮件、找到批量主题工具、再次输入主题。

新设计在用户填写一封原本空白的主题后立即检查同批其他邮件：

- 如果没有其他空白主题，不出现任何额外界面；
- 如果还有空白主题，在当前主题框下询问是否使用相同主题；
- 一键操作只补空白主题，绝不覆盖已有主题；
- 被排除的邮件不会被修改；
- 自动补上的其他邮件仍保持“待确认”，不会被自动放行。

## 编辑保留与确认分离

切换邮件、字段失焦或收起预览时，当前编辑先保留在本批次中。它不会因此变成“已确认”。

按钮改为：

- **确认本封**
- **确认并下一封**

这样“防止编辑丢失”自动发生，而“确认内容正确”仍由用户明确完成。

## 尺寸与重叠

在当前 1320px 最大工作台宽度下，批量页面还要同时容纳左侧一级导航和右侧流程栏，因此检查区不再强行三栏并排：

- 常规宽度：约 188px 邮件列表 + 完整内容区；
- 中等宽度：邮件列表缩至约 168px；
- 小窗口：邮件列表、原文、编辑区顺序单列；
- 最终创建区保持普通文档流，不增加第二个固定层。

## 保留边界

只创建草稿，不自动发送；已有主题不会被上下文批量操作覆盖；自动安排时间仍需用户点击“应用”后才写入任务；编辑自动保留不等于自动确认。
