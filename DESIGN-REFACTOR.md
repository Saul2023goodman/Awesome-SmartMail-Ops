# v1.16 界面重构说明

## 核心判断

用户的目标不是使用“导入、确认、批量填写、排程”等多个工具，而是完成一件事：**把一批邮件变成可以安全创建的草稿。**

因此新版按实际操作组织界面：

**添加资料 → 检查邮件 → 选择与安排 → 创建草稿**

## 为什么解析预览不独立

解析预览只在资料导入后有意义。它的任务是让用户确认系统有没有正确读出收件人、主题和正文，然后继续处理这个批次。

如果把它做成一级工作区，用户需要离开当前批次再进入“确认”，完成后还要返回批量工作台；同一批数据也会出现两套入口。因此新版把解析预览放回导入结果之后，需要时展开，处理完继续向下。

## 为什么批量填写不独立

批量填写不是一个长期工作区，而是用户在检查邮件时发现重复问题后的快捷动作。

新版只有选中邮件后才显示批量操作：可以补充所选邮件的空白主题，也可以一次保存所选修改。没有选择时不展示这些按钮，减少界面噪音。

## 为什么选择与安排合并

用户通常先决定“这批要做哪些邮件”，随后决定“这些邮件什么时候创建”。两者围绕同一批选中对象，不需要拆成两个页面或两个大功能区。

因此新版把排时间放进选择区，并默认折叠。没有排程需求的用户可以直接跳过。

## 为什么继续保留右侧流程

批量页仍然是长页面，所以流程导航保留在右侧并随页面滚动。它只负责告诉用户当前位置和快速跳转，不再承载额外功能。

为了避免挤压：

- 宽屏显示步骤名称；
- 中等窗口缩成窄编号栏；
- 小窗口改为顶部横向步骤；
- 页面只保留这一处固定导航，最终创建区回到正常文档流中，不再形成第二个悬浮层。

## 尺寸与信息密度

新版重新检查了常见窗口宽度下的布局，并做了这些收敛：

- 一级导航从四项减为三项；
- 批量表格从九列减为六列；
- 解析预览只保留邮件检查真正需要的字段；
- 原文、队列、编辑区在空间不足时自动换行，而不是强行三栏并排；
- 可选资料和低频设置折叠；
- 删除重复的准备完成摘要、抽查入口和常驻批量工具。

## 保留的边界

界面可以更简单，但保存安全不变：只创建草稿，不自动发送；上一封确认保存成功后才继续；出现错误立即停止。

## v1.16 的性能原则

界面结构不再变化，优化重点转为“同样的用户动作，用更少的计算和绘制完成”：切换页面不重算未变化数据；隐藏页面不提前渲染；搜索与保存合并处理；大列表分段显示；高成本核对建立索引；固定区域不再使用实时背景模糊。
