# v1.24 收件人归属与跨邮件边界修复

## 问题现象

同一份 Word 邮件资料在 v1.19 / v1.20 中能够识别大部分收件人，升级到 v1.21 结构解析后却出现“原文附近明明能看到邮箱，但收件人字段为空”的回退。

在当前复现资料中：

- 邮件结构：63 封
- 原文邮箱：58 个
- v1.19 / v1.20：58 / 63 封识别出收件人
- v1.21 ~ v1.23：0 / 63 封识别出收件人
- v1.24 修复后：58 / 63 封识别出收件人

剩余 5 封原资料本身没有可确定邮箱，继续保留人工补充，而不是猜测。

## 根因

v1.21 为解决“上一封邮件尾部的来源邮箱串到下一封收件人”增加了 `consumedEndBlock`：上一封邮件不仅记录真正的邮件终点，还记录落款后被隔离的来源、备注等尾部内容；下一封邮件搜索收件人时从 `previousConsumedEnd + 1` 开始。

方向本身正确，但“尾部隔离范围”和“下一封邮件的前导区”没有区分，导致范围所有权错误。

典型来源顺序：

`上一封结束语 / 署名`

→ `契合点 / 改写说明`

→ `📧 下一位导师邮箱`

→ `编号 / 对象标题`

→ `Subject`

旧逻辑把“下一位导师邮箱”也当成上一封邮件落款后的隔离内容，`consumedEndBlock` 因而越过真实邮箱。等下一封邮件再查找收件人时，该邮箱已经在搜索起点之前，于是整批出现“邮箱可见、字段却为空”。

另一个变体是：

`### 1. Jin Li — jli1@hku.hk`

这种 Markdown 对象标题同时包含邮箱。v1.21 的通用 Markdown 噪声规则先于“对象标题”判断执行，导致标题被上一封尾部作为说明吞掉，同样使下一封失去邮箱。

## 系统性修复

### 1. 收件人是下一记录的前导字段，不是上一封邮件的尾部 sidecar

在结束语之后遇到明确的收件人字段时立即结束上一封邮件的尾部所有权，不把该块加入 `excludedBlocks`，也不推进 `consumedEndBlock`。

来源、附件、定时、内部说明仍然可以归属于上一封邮件并被隔离，因此 v1.20 的防串线能力保留。

### 2. 支持“邮箱 + 人工备注”的明确收件人行

`📧 name@university.edu（请以官网为准）` 仍然是明确收件人线索。解析不再要求整段只能包含一个邮箱。

### 3. 先判断对象标题，再判断通用 Markdown 噪声

`### Name — email@university.edu` 是对象身份块，不是普通说明标题。身份语义优先于展示格式，避免 Markdown 层级改变业务含义。

### 4. 开放式邮件同样遵守收件人边界

即使上一封缺少结束语，只要正文之后出现新的明确收件人字段，也停止上一封的正文/尾部消费，防止下一记录被吞并。

## 不回退的安全约束

修复不能简单退回 v1.19 的“向前扫最近邮箱”，因为那会重新引入来源邮箱串线。

因此保留以下规则：

- `Research source: wrong-recipient@example.edu` 仍属于来源，不可成为下一封收件人。
- 两个同等可信、无法区分的邮箱仍保持空值并进入人工确认，不静默任选。
- 落款后的来源、附件、定时、说明仍从正文中隔离。
- 真正没有邮箱的记录继续显示缺少收件人，不从其他导师或其他记录猜测。

## 回归测试

新增 `test-recipient-ownership.js`，覆盖：

1. 上一封说明之后、下一封编号之前出现 `📧 下一收件人`。
2. Markdown 对象标题内嵌邮箱。
3. 上一封来源字段含邮箱时不得串到下一封。

同时继续运行原有边界、来源分流、业务状态、自动推进、排程与 UI contract 测试。
