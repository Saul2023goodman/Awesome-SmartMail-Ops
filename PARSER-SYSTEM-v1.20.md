# v1.20 统一邮件正文边界系统

## 问题根因

截图中的 `Research source` 已经在解析依据中位于落款和姓名之后，却仍进入最终正文。根因不是单个英文短语漏配，而是旧解析器采用了以下位置假设：

1. 找到落款后，无条件再收取最多两段短文本。
2. 第一段通常是姓名，因此规则在普通样本上看似有效。
3. 第二段只检查长度、邮箱、Subject 和少量说明词；`Research source` 较短且不在旧词表中，于是被当作第二行签名。
4. 任务生成层直接信任识别器给出的正文，没有再次核对“正文终点”和“原文边界”是否一致。
5. Word 字段式回退解析在进入 `body` 状态后，只遇到已知业务字段才退出；未知的来源或说明标签也会继续追加到正文。

因此这是“边界模型缺失”，不是添加一个 `Research source` 正则即可解决的个例。

## 旧系统的系统性缺陷

| 缺陷 | 可能后果 |
| --- | --- |
| 按固定行数推测签名 | 短来源、备注、链接进入正文；较长真实签名反而被截断 |
| 各适配器分别猜边界 | DOCX 修好后，TXT、HTML 或字段式 Word 仍可复发 |
| 只输出正文字符串 | 不知道哪些内容被排除、为何排除，无法解释或复核 |
| 只识别邮件起点，不对尾部分类 | 附件、定时说明、收件人线索、下一记录可能与正文混在一起 |
| 收件人候选不理解来源段 | 来源里的邮箱可能串到下一封邮件 |
| 无生成前一致性校验 | 上游一次误判会直接流入草稿 |
| 同一文本块只判断一次 | `落款 + 姓名 + 来源` 同段混排时无法在块内切分 |

## 新的解析路径

所有可转成有序文本块的来源统一经过同一个邮件识别核心：

`格式适配 → 有序原始块 → 锚点识别 → 块角色分类 → 正文装配 → 尾部隔离/字段分流 → 任务前净化 → 可解释证据`

格式适配器只负责保持内容顺序，不再自行定义一套邮件边界。明确的 CSV/XLSX/JSON 正文字段继续按用户提供的数据原样保留；只有系统自动识别出来的正文才进入边界净化，避免擅自修改用户明确填写的内容。

## 统一角色与处理原则

| 角色 | 处理 |
| --- | --- |
| Subject / 称呼 / 正文 / 落款 | 写入对应邮件结构 |
| 签名 | 保留；支持姓名、身份、院系、电话、邮箱、个人网站等多行形式 |
| 附言（P.S. / 附言 / 又及） | 保留，并继续读取后续附言内容，直到出现强边界 |
| 来源 / 参考资料 / 导师主页 | 从正文隔离，保留为来源证据 |
| 附件清单 | 从正文隔离，并转入附件字段，由现有附件缺失检查继续处理 |
| 定时说明 | 从正文隔离，并转入定时字段，由现有时间解析继续处理 |
| 内部备注 / 改写说明 / 契合点 | 从正文隔离，不创建草稿内容 |
| 下一 Subject / 编号 / 人物—院校标题 | 结束当前邮件，进入下一记录上下文 |
| 落款后未归类文本 | 暂不写入正文，同时产生待确认原因，避免静默误发 |

## 关键防错机制

### 1. 强边界优先于长度

`Research source:`、`资料来源：`、`References:`、`Professor profile:` 等以语义标签判定，不再因为文本较短而被当作签名。

### 2. 签名采用正向识别

不再“凡是短文本都算签名”，而是确认其符合姓名、身份、单位或联系方式等签名特征。这样既允许多行签名，也减少短备注混入。

### 3. 模糊项不静默处理

明确来源可自动隔离；落款后的普通未知句子则标为“未归类尾部”，进入人工确认。系统在准确性和信息保全之间采用可见的安全降级。

### 4. 块内二次切分

Word 换行、HTML 文本块或粘贴文本可能把落款、姓名和来源放在同一块。新系统会在块内按换行继续分类，而不是整块一起写入正文。

### 5. 跨记录污染抑制

来源、附件、定时或备注段里的邮箱会显著降权，低于可靠阈值时不自动成为收件人；这些段落也不会被选作下一封邮件的身份标题。

### 6. 任务生成前第二道校验

自动识别正文在进入任务前再次经过相同的尾部净化。该防线只作用于 `mailFrames` 自动识别结果，不触碰人工编辑或明确的表格正文字段。

## 举一反三覆盖范围

- 英文来源：`Research source`、`Sources`、`References`、`Citation`、`Professor profile`、`Official page`。
- 中文来源：`资料来源`、`研究来源`、`信息来源`、`参考资料`、`参考文献`、`导师主页`。
- 业务元数据：`Attachments`、`Required attachments`、`Scheduled send time`、`Recipient email`。
- 编辑元数据：`Internal note`、`Instructions`、`Rewrite notes`、`契合点`、`改写说明`、`发送说明`。
- 结构边界：下一 Subject、单独编号、人物—院校标题、说明 emoji、分隔线。
- 应保留内容：多行签名、个人联系方式、个人网站、P.S./附言、正文中普通出现但不作为标签的 “research source” 词组。

## 回归验证

`test-parser-boundaries.js` 覆盖：

1. 截图中的 `Research source` 污染。
2. 中英文来源及教授主页变体。
3. 附件和定时时间分流。
4. 多行签名、个人网站和 P.S. 保留。
5. 无落款邮件的尾部来源。
6. 落款、姓名、来源同一文本块。
7. 称呼、正文、来源同一文本块。
8. 来源邮箱不串成下一封收件人。
9. 落款后模糊文本进入待确认。
10. 原有业务状态、自动推进、UI、排程与性能回归测试。
