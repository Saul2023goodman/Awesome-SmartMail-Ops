# NetEase Mail Draft Assistant v2.1 · UI Reflow

## 目标

本轮只重构用户界面与页面组织，不改变导入解析、联系人判断、附件匹配、排期算法、background / executor 通信和“只创建草稿、不自动发送”的安全边界。

核心问题不是“颜色不好看”，而是旧工作台把多个业务阶段长期堆在同一张长页面里，再用右侧流程栏、悬浮动作和逐版本 CSS 压缩去容纳更多功能。结果是信息密度失控、字号越来越小、窄窗口靠挤压而不是重排、多层滚动互相争夺输入焦点。

## 业务流程

批量草稿只保留一条前进路径：

1. **导入资料**：加入邮件文件 / 文件夹 / 粘贴内容；需要时补参考总名单与附件资料。
2. **核验待办**：只处理机器无法确认或用户需要修正的邮件、附件与文件用途。
3. **选择与排期**：确认本次要创建的邮件，设置同校错峰、间隔、节假日规则和逐封时间。
4. **创建草稿**：最后确认本次选择，然后把任务交给既有 background / executor 创建并保存草稿。

“单封草稿”和“联系人”是独立工作区，不嵌入上述批量四步流程。

## UI 设计规则

### 1. 一个页面只承担当前任务

批量页由 `data-view-step` 控制阶段视图。阶段 1/2 使用导入与核验工作区；阶段 3 使用选择与排期；阶段 4 只保留最终邮件清单和创建动作，不重复显示完整排期表单。

流程条从原右侧长轨道改为顶部横向步骤器。它既表示完成度，也允许返回已解锁阶段，但不通过 `scrollIntoView` 在长页面里跳位置。

### 2. 外层页面不滚动

`.nmda-page` 固定在浏览器可用高度内并 `overflow: hidden`。滚动只交给确实需要浏览大量内容的业务模块：

- 导入 / 待办：当前导入或核验模块；
- 窄窗口排期：排期主卡片；
- 联系人：联系人表格；
- 单封正文：正文输入框本身。

同一操作路径避免纵向嵌套滚动；打开模态核验时，背景模块停止滚动。

### 3. 窄窗口不是桌面三栏的缩小版

- 主导航从 208px 收缩到图标栏，而不是压缩文字到不可读。
- 文件用途核验：宽屏为“目录 / 文件 / 详情”三栏；820px 时改成“目录 + 文件列表”，点击文件后用详情替换列表，并提供返回按钮。
- 单封草稿在 820px 仍保留“正文 + 发送选项”双栏，避免变成长页面。
- 排期在 820px 由当前阶段卡片独立滚动，页面本身不滚动。

### 4. 视觉层级服务操作，而不是解释技术

- 基础正文与表单字号提升到约 11–14px 区间，标题层级更明确。
- 主动作统一蓝色；成功/完成使用绿色；需确认使用暖色；附件使用紫色；中性信息使用灰色。
- 主导航使用同一套线性 SVG 图标，不再混用字符、Emoji 与不同视觉重量的符号。
- 大量“为什么系统这样做”的说明从默认界面移除，只保留当前动作所需的一句提示。
- `hidden` 被定义为最高优先级可见性契约，任何布局规则都不能把已经隐藏的页面重新显示。

## 代码结构

- `app.js`：保留原业务函数与状态，只新增/调整 `uiStep`、流程导航与少量用户文案。
- `ui-system.css`：v2.1 的统一布局与视觉系统，最后加载，负责页面尺寸、阶段切换、响应式、字号、颜色、滚动所有权和模态布局。
- `app.css`：保留原组件细节，避免本轮 UI 重构误伤解析/编辑/附件等既有功能。
- `qa/ui_audit.py`：增加页面级滚动、嵌套滚动、隐藏页面复活、超小控件、滚动模块塌陷和跨主模块响应式检查。

## QA 覆盖

自动测试真实加载生产 UI 与导入解析栈，并导入 Word + Excel + PDF 样本。

来源核验弹窗覆盖：1440×900、1280×800、1100×760、920×720、820×700。

额外覆盖：

- 选择与排期：1440×900 / 820×700
- 创建草稿：1440×900 / 820×700
- 单封草稿：1440×900 / 820×700
- 联系人：1440×900 / 820×700

当前自动布局检查 P0 = 0，P1 = 0。
