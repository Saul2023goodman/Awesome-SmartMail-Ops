# UI Reflow v2.4 · Dialog Workflow

## 1. 这轮反馈真正指出的问题

本轮不是“联系人表格太矮”或“排期缺少返回按钮”两个孤立问题，而是上一版仍然把过多的**浏览、编辑、配置、审阅**操作放在同一个页面平面里。

结果有三个典型症状：

1. **流程变成单向隧道**：进入“选择与排期”后，邮件审阅被视为上一阶段，只能顺着步骤前进，无法在当前上下文中再次打开审阅。
2. **高频浏览被低频编辑挤占**：联系人列表每一行都长期显示阶段、策略、标签、长期标记等编辑控件，真正需要快速扫描的联系人信息只能显示很少几行。
3. **永久占位过多**：排期规则、筛选、说明、状态和列表同时常驻。即使每个模块都“合理”，叠加后仍会吞噬主工作区。

因此，问题的核心不是继续压缩尺寸，而是重新划分**信息层级和交互层级**。

---

## 2. 新原则：主页面浏览，弹窗决策

v2.4 将界面分成两类空间。

### 主工作区

主工作区只承担：

- 总览
- 扫描
- 选择
- 比较
- 查看当前状态
- 发起下一项操作

它必须优先把空间留给“对象本身”，例如邮件列表、联系人列表，而不是把每个对象的全部编辑器长期展开。

### 任务弹窗

弹窗承担：

- 单封邮件深度审阅
- 排期规则设置
- 单联系人状态、策略、标签编辑
- 联系人的发送/草稿历史查看

这些操作都是**临时进入、完成后返回原处**的任务，不应永久占据主画布。

这不是为了“多做弹窗”，而是建立一条约束：

> 只有需要持续对照多个对象的信息才留在页面；只针对一个对象或一次设置的复杂操作进入弹窗。

---

## 3. 流程重新定义：阶段 ≠ 工具

批量流程仍然保持：

`导入资料 → 核验待办 → 选择与排期 → 创建草稿`

但 v2.4 不再把“邮件审阅”理解成只能存在于第 2 步的页面。

阶段描述的是**批次生命周期**；审阅、排期设置描述的是**可在当前阶段调用的工具**。

因此在第 3 步：

- 用户可以继续查看邮件列表；
- 点击任意邮件的“审阅”，打开全屏审阅弹窗；
- 点击顶部“审阅邮件”，进入当前批次审阅队列；
- 关闭审阅后，原地返回第 3 步，当前排期状态、选择状态不丢失；
- 点击“排期设置”打开规则弹窗；
- 应用或关闭后，同样原地返回邮件列表。

这修复了“进入排期就无法回头”的单向流程问题，同时避免用一个额外“返回上一阶段”按钮破坏已经完成的阶段状态。

---

## 4. 排期：从规则页面改成邮件优先工作台

### 主页面只留下

- 本批次概要
- 当前排期规则摘要
- 当前选中范围
- 必要筛选
- 邮件列表
- 每封邮件的发送时间与结果
- `审阅邮件`
- `排期设置`

### 排期设置进入弹窗

以下内容不再永久占位：

- 开始时间
- 每所院校/导师限制
- 间隔天数
- 保留已有时间
- 避开周末/节假日

弹窗关闭后不改变用户在邮件列表中的位置。

### 这样做的原因

排期阶段最频繁的问题不是“规则是什么”，而是：

> 哪些邮件要发？什么时候发？哪一封需要重新看？

因此列表才应得到最大面积；规则只是影响列表结果的配置工具。

---

## 5. 邮件审阅：从阶段内容改成全屏任务弹窗

审阅弹窗采用：

- 左侧：当前批次审阅队列
- 右侧：单封邮件编辑/核验
- 顶部：当前任务身份与返回动作
- 正文编辑器自动吃满剩余高度

只让队列和正文编辑区承担必要滚动，外壳不制造第二层页面滚动。

关键行为：

- 从第 2 步打开，关闭后回第 2 步；
- 从第 3 步打开，关闭后回第 3 步；
- 点击排期邮件行的“审阅”，直接定位到该邮件；
- 审阅是上下文工具，不再强制改变批次阶段。

---

## 6. 联系人：从“表格内编辑器”改成“高密度列表 + 单联系人弹窗”

旧设计的问题是每一行同时放：

- 阶段下拉
- 关键联系人标记
- 策略下拉
- 长期标记输入
- 已发送统计
- 草稿统计
- 最近活动

这些控件在 80 个联系人时几乎全部是低频编辑，却永久消耗每一行高度和横向宽度。

v2.4 主联系人页只显示：

- 联系人
- 当前状态
- 最近活动
- 已发送数
- 草稿数
- 查看入口

点击联系人后打开详情弹窗，在弹窗内编辑：

- 阶段
- 策略
- 跟进状态
- 标签/长期标记
- 已发送历史
- 草稿历史

因此联系人页重新成为“扫描 80 个联系人”的页面，而不是“同时编辑 80 个联系人”的表单。

---

## 7. 从本轮反馈总结出的长期 UI 规则

### 规则 A：拆模块不是画更多卡片

真正的模块拆分要改变：

- 可见性
- 状态所有权
- 导航关系
- 空间所有权

如果几个卡片仍同时常驻一个画布，它们只是视觉分组，不是真正拆分。

### 规则 B：列表中的对象不要自带完整编辑器

高数量对象页面默认应该是浏览模式。复杂编辑器应按需打开，否则页面密度一定失控。

### 规则 C：阶段流程必须允许上下文回访

线性阶段用来描述完成度，不应禁止用户重新查看前面的信息。审阅、详情、规则这类操作应成为上下文工具，而不是强迫用户回退整个阶段。

### 规则 D：固定区域必须持续产生价值

一个控件只有在“几乎每一秒都需要”时才值得永久占位。低频规则、说明、编辑器应该折叠、弹窗化或按需出现。

### 规则 E：滚动属于长内容，不属于应用外壳

允许：

- 邮件列表滚动
- 联系人列表滚动
- 审阅队列滚动
- 正文编辑区滚动
- 历史记录滚动

避免：

- 页面外层滚动
- 弹窗外壳 + 弹窗内容重复滚动
- 表格横向/纵向/页面纵向三重滚动

### 规则 F：响应式优先“延后显示”，不是“继续压缩”

空间不足时优先：

1. 隐藏次要列；
2. 将复杂编辑移入弹窗；
3. 将低频设置移入弹窗；
4. 保留核心对象与主操作；
5. 最后才微调字号和间距。

---

## 8. 代码结构变化

- `app.js`
  - 新增审阅返回上下文 `reviewReturnStep`；
  - 审阅工作区从第 2 步内容树中脱离，挂载到全局面板层；
  - 新增排期设置弹窗控制；
  - 新增联系人详情弹窗及保存逻辑；
  - 排期邮件行新增直接审阅入口；
  - 联系人渲染改为浏览型行，不再行内塞完整编辑器。

- `workflow-dialogs.css`
  - 新增弹窗层与滚动所有权规则；
  - 排期页改为列表优先布局；
  - 审阅改为全屏任务弹窗；
  - 联系人改为高密度列表；
  - 820×700 与 1366×611 下按重要性隐藏次要列，而不是挤压全部列。

- `qa/ui_audit.py`
  - 新增从排期打开审阅并原地返回的状态断言；
  - 新增排期设置弹窗截图；
  - 新增联系人详情弹窗截图；
  - 继续覆盖 1440×900 / 1280×800 / 1100×760 / 920×720 / 820×700 / 1366×611。

---

## 9. 本轮不改变的业务边界

本轮只改变 UI 信息架构、页面/弹窗关系和交互状态，不改变：

- 邮件解析规则
- 文件用途识别
- 附件库逻辑
- 总名单逻辑
- 查重逻辑
- 排期算法
- 联系人数据模型
- 网易邮箱执行器
- 草稿创建行为
