# v1.30 Decision Canvas

## 问题判断

本轮从三个用户反馈出发：

1. “更多”菜单里只有“排除”一个功能，增加一步没有带来信息组织收益。
2. “识别依据”要求用户理解系统内部如何解析邮件，而用户真正需要的是判断邮件内容是否正确。
3. 多篇选一如果只能逐封“查看内容”，用户必须在脑中记住上一封的主题、正文和差异，比较成本很高。

由此形成一条统一原则：**核心动作直接可见；技术过程退出用户主流程；比较型决策让候选同时可见。**

## 直接落地

### 1. 排除从菜单恢复为一等操作

“排除此封”放在当前邮件工具栏，和上一封/下一封处于同一上下文，但使用红色描边降低误触。它不会悬浮覆盖正文，排除后仍可通过“恢复已排除”撤销。

### 2. 删除识别依据

Review Canvas 不再渲染：

- 邮件起点/终点
- 称呼/落款识别标签
- 解析置信度
- 原始块上下文
- “识别依据”折叠面板

这些数据仍可服务解析器、诊断或测试，但不参与普通用户决策。

当收件人有多个候选时，不再要求打开证据，而是在收件人字段旁直接列出最多 5 个可选邮箱。

### 3. 重复邮件变成 Comparison Canvas

重复组的候选不再是摘要行，而是并排预览卡：

- 主题
- 收件人
- 来源
- 正文长度
- 附件数量
- 完整正文可滚动预览
- “保留此封”勾选
- 需要修改时使用“编辑这封”

2 封和 3 封默认同屏；4 封及以上改为横向比较，不让卡片压缩到无法阅读。

### 4. 选择与编辑解耦

“选择保留哪些”与“编辑其中某一封”是两件事。

用户可以先勾选 2/3 封，再点击其中一封“编辑这封”。切换编辑对象后，之前的保留集合继续保留。最终“保留所选（N）”一次执行整组取舍。

## 举一反三规则

后续所有类似场景使用同一判断：

- **一个菜单只有一个动作**：优先直接展示，不创建“更多”。
- **用户只需要结果，不需要理解算法**：技术证据不进入主界面。
- **需要在 A/B/C 中取舍**：优先并排比较，而不是下拉选择或逐个打开。
- **候选信息本身很短**：直接平铺成 chips/cards，不增加二级弹层。
- **危险但高频的核心动作**：直接可见 + 明确危险样式 + 可撤销，而不是深藏菜单。
- **推荐值**：说明“为什么更适合”，避免用置信度数字制造虚假精确感。

适用到本插件其它功能时：候选收件人已采用直接平铺；批量邮件本身继续使用可同时扫描的列表/表格；未来若出现多个附件版本、多个主题候选、多个联系人归属候选，也应复用 Comparison Canvas，而不是新增独立弹窗。

## Runtime 验证

Chromium VM 已验证：

- 1366×612、53 封邮件：`排除此封`直接可见，正文无遮挡，识别依据不存在。
- 1366×768、3 封重复：3 封正文同时可见。
- 初始默认勾选 1 封；再勾选第二封后按钮实时变为“保留所选（2）”。
- 切换“编辑这封”后，两封勾选状态保持。
- 确认保留 2/3 后，“恢复已排除（1）”正确出现。
- 页面级 JavaScript error = 0。
- 全部现有 JS 回归测试通过。
