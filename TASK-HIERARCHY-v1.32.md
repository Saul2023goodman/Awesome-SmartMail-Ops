# v1.32 · Task Hierarchy / 任务优先级重构

## 问题来源

用户截图暴露的核心不是单一文案问题，而是业务权重与视觉权重不一致：

- **参考总名单**属于可选增强，却使用黄色大卡和大量解释，像一个必须处理的风险项。
- **附件缺失**会真实阻塞草稿创建，却只藏在“附件候选 / 附件待加”的统计文字里。
- “读取完成 / 自动分流 / 读取提示 / 来源组数”属于系统过程，却占据首屏核心位置。
- 同一个导入结果被“顶部描述 → 一行统计 → 黄色读取提示 → 资料自动分流”重复表达。
- 已进入流程 2 时，主视图仍主要展示流程 1 的导入细节，造成“右侧说我在处理待办，正文却还在讲导入”的状态错位。
- 多个蓝色按钮同时出现时，用户无法判断当前真正应该做什么。

## 业务信息重新分级

### A. 阻塞项 Blocker
会阻止下一阶段：缺收件人、缺主题、重复取舍、人工核对、附件缺失。

设计原则：
- 进入“当前待办”；
- 明确数量与结果；
- 提供直接操作；
- 当前最高优先级才获得 Primary CTA。

### B. 增强项 Context
不会阻止基础流程，但提前提供可降低返工：参考总名单。

设计原则：
- 使用蓝灰轻量提示，不使用警告色；
- 说明一句核心收益即可；
- 必须允许明确跳过；
- 用户做出选择后压缩成状态行；
- “补充名单”在已跳过/已加入状态降级为次级动作。

### C. 系统过程 System detail
自动分流、来源组数、读取提示、文件格式等。

设计原则：
- 默认不占首屏；
- 统一收进“导入详情”；
- 只有读取失败/没有识别出邮件时才升级为用户可见错误。

## 具体改动

### 1. 已导入状态改为轻量工具栏

导入完成后：

- “选择文件 / 选择文件夹 / 粘贴内容”改成紧凑的“添加文件 / 添加文件夹 / 粘贴补充”；
- 不再继续用三张大入口卡占据流程 2 首屏；
- 顶部只保留“已加入 N 个文件 · N 封邮件”。

### 2. 参考总名单去警告化

Pending：

> 参考总名单可减少重复联系  
> 有名单就现在加入；没有可直接跳过，不会影响当前批次查重。

Added：

> 参考总名单 · N 条  
> 已用于联系人核对和院校排期。

Skipped：

> 未使用参考总名单  
> 当前批次仍会正常查重，需要时可随时补充。

不再显示：
- “建议 · 进入待办前补充”；
- 三枚收益标签；
- “基础查重保持启用”等重复状态；
- 技术性的名单来源状态。

### 3. 导入详情默认折叠

将“资料已自动分流”和多张来源卡收进：

> 导入详情 · N 个文件 · N 封邮件

默认关闭。用户只有在怀疑系统把文件用途识别错时才展开。

读取 warning 同样移动到导入详情内部，不再因为存在非关键 warning 把整条导入成功状态染成黄色。

### 4. 附件升为一级待办

Step 2 首屏直接出现：

> **还有 N 个附件待补**  
> 这是创建前必须完成的待办。

同时提供：
- 添加附件文件夹；
- 选择文件；
- 稍后处理（仅在还有更高优先级邮件问题时出现）。

附件统计使用独立强调样式，不再和普通解析统计等权。

### 5. 附件详情归入待办中心

原本“附件”details 位于“本批次资料”中，会造成：

- 导入区和待办区重复出现附件；
- 用户误以为附件属于导入动作，而不是流程 2 的阻塞项。

现在“附件明细”整体移动到“当前待办”下，仅用于查看匹配结果/手工指定文件，不再重复承担一级提醒职责。

### 6. CTA 随业务优先级变化

- 参考名单 Pending：`上传总名单` 是 Primary；附件 CTA 不出现。
- 进入 Step 2 且仍有邮件问题：`继续处理待办` 是唯一 Primary；附件上传降为 Secondary。
- 只剩附件：`添加附件文件夹` 自动成为 Primary。
- 附件补齐：自动进入“选择与安排”。

因此“重要”不等于“同时全部高亮”。

## 文案原则

从：

> 读取完成：文件夹（13 个可读取文件）；已分流为邮件 1 组，附件候选 1 组，暂不使用 2 组；20 条读取提示……

转为：

> 已加入 13 个文件 · 1 封邮件

系统分流细节仍保留在“导入详情”。

从：

> 检测到 3 个附件要求，还有 3 个未匹配

转为：

> 还有 3 个附件待补

从“系统做了什么”转成“用户现在需要处理什么”。

## 回归验证

- 17 组 JS contract / regression tests 全部通过。
- Chromium VM 完整复杂旅程通过：导入 → 参考名单选择 → 邮件待办 → 重复决策 → 附件 → 选择与安排 → 排期。
- 页面级 JavaScript error = 0。
- Step 1 只出现参考名单 Primary CTA；附件不会提前抢主操作。
- Step 2 有邮件 blocker 时，附件明确显示但不争夺 Primary CTA。
- 邮件 blocker 清零后，附件 CTA 自动升级为 Primary。
- 导入详情默认关闭。
