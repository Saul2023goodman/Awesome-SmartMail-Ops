# v1.21 邮件结构定位升级

## 截图错误

原文尾部是：

`此致` → `敬礼—` → `孙潇阳`

旧状态机先命中 `此致\n敬礼`，再把剩余的单个破折号 `—` 当成未知内容。遇到未知内容后它立即停止读取，所以姓名没有机会进入署名判断，最终出现三个相互关联的错误：

1. `敬礼—` 被标成“邮件终点”；
2. 同一段又被标成“已排除·未归类尾部”；
3. `孙潇阳` 位于邮件范围之外，无法成为署名。

这不是中文姓名词库不足，而是结束语命中后的剩余字符处理发生短路。

## 旧解析方式的共同缺陷

| 旧假设 | 可能错误 |
| --- | --- |
| 每个段落只属于一个角色 | 结束语、破折号、姓名同段时互相覆盖 |
| 找到关键词就认定边界 | 正文中的 `Many thanks for...` 被误认为结束语 |
| 一个段落内完成字段 | `Research source` 与下一行链接无法组成来源字段 |
| 未知尾部立即停止 | 排版符号挡住后面的真实署名 |
| 界面重新用正则贴标签 | 解析结果正确但“解析依据”仍显示错误 |
| 相同文本不考虑位置 | 主题值 `123` 被误当成记录编号 |
| 多个邮箱直接选最高分 | 分数接近时可能选错收件人且用户不知情 |

## 新结构模型

系统先保留原始块顺序，再按以下序列判断：

`对象标题 / 收件人线索 → 主题 → 称呼 → 正文 → 结束语 → 署名 → 附言 → 来源 / 附件 / 时间 / 说明 → 下一封邮件`

每个原始块可以具有多个角色。例如：

- `Yours sincerely,\nXiong Guo` 同时包含结束语和署名；
- `Subject: Enquiry\nDear Professor...` 同时包含主题与称呼；
- `此致\n敬礼—\n孙潇阳` 同时包含结束语、署名和邮件终点。

## 处理原则

### 1. 结束语按短语形态和位置判断

只有独立、简短的结束语或“结束语 + 分隔符 + 姓名”才作为邮件尾部。正文中的完整句子不会因出现 `Many thanks` 或 `Best regards` 就被截断。

### 2. 排版符号不改变语义

结束语后的破折号、冒号、星号等只作为排版残留跳过；解析器继续寻找姓名、身份、院系、联系方式和个人网站。

### 3. 跨段组成一个字段

来源、附件、发送时间和说明既可写成 `标签：值`，也可使用“标题段 + 若干值段”。明确字段自动分流；普通未知文字仍安全隔离并要求人工确认。

### 4. 位置优先消除同形歧义

- `主题：123` 中的 `123` 是主题；
- 两封邮件之间的单独 `123` 才可能是记录编号；
- 签名区的邮箱是发件人联系方式，不会回流为收件人；
- 来源区的邮箱不会成为下一封邮件的收件人。

### 5. 收件人只在证据足够时自动选择

明确的“收件人/教授邮箱”标签、邮箱图标、姓名匹配和邻近位置会提高分数；来源、备注和发件人语境会降低分数。两个不同邮箱同样可信时不静默任选，而是展示候选供用户判断。

### 6. 解析依据直接使用结构结果

识别器为每个原始块输出实际角色。界面只渲染这些结果，不再自行猜测，因此正文切片、邮件终点、排除原因和可视标签保持一致。

## 举一反三覆盖

- 中文结束语：同段/跨段 `此致、敬礼`，以及 `祝好、顺颂时祺、敬颂教祺、谨致问候` 等短格式。
- 英文结束语：`Yours sincerely, Sincerely, Best/Kind/Warm regards, Best wishes, Respectfully` 等独立格式。
- 署名：中英文姓名、多行身份/院系、电话、邮箱、网站，同段或跨段均可。
- 字段标题：`Research source / Sources / References / 资料来源 / 附件清单 / Scheduled send time / 发送说明`，支持下一行继续。
- 表现层标记：Markdown 粗体、标题、列表符号不改变字段角色。
- 安全降级：无法确认的落款后文本保持隔离并进入待确认，不静默写入或删除。

## 验收重点

1. 截图样本的邮件终点落在 `孙潇阳`。
2. `敬礼—` 不再显示“已排除·未归类尾部”。
3. `Research source` 及其跨行链接不进入正文。
4. 附件和时间跨行时进入对应字段。
5. 纯数字主题被保留。
6. 正文中的感谢或问候句不触发提前截断。
7. 多个相近邮箱进入待确认，不自动错选。
8. 原有自动通过、主题补齐、附件阻塞、自动推进、UI 与性能逻辑保持不变。
