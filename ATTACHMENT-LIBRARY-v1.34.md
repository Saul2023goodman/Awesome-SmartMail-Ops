# v1.34 · Attachment Library

## 1. 本轮问题的实质

用户指出的“阴影不全、字体不居中、上传后看不到附件”实际上来自两个不同层面的错误：

1. **视觉容器错误**：批次准备 Dialog 自己承担滚动，阴影和滚动表面耦合；同时文件选择按钮使用 `<label>`，普通动作使用 `<button>`，基础 `.nmda-btn` 没有统一 flex 对齐，造成基线和高度不一致。
2. **业务对象错误**：附件被建模成“上传动作”，而不是“批次资产”。系统只留下数量和匹配结果，用户无法持续回答“我上传了什么、哪一封在使用、还能否删除或替换”。

因此本轮不是单纯补一个文件名列表，而是把附件改造成贯穿整批任务的单一资产库。

## 2. 新业务模型

### 导入完成时

邮件解析完成后进入“补充本批次资料”。附件 Box 永久存在：

- 有明确附件需求：显示需求文件名；
- 已选择附件：立刻显示附件列表；
- 无明确需求：仍可提前加入 CV、成绩单等文件；
- 没有附件：可以跳过。

### 附件库中的每一项

每个附件展示：

- 文件名；
- 文件大小；
- 来源（附件文件夹 / 手动添加 / 导入资料 / 每封都附加）；
- 当前业务使用状态（已匹配 N 封 / 待匹配 / 每封草稿）；
- 单项“移除”。

这比“已选择 3 个文件”更接近用户真正需要核对的信息。

### 完成准备以后

附件入口不会消失：

- 上传页“批次准备”中的附件状态提供“查看 / 修改”；
- 处理待办时，如果附件尚缺，待办卡直接提供“管理附件”；
- 如果附件已经全部匹配，仍显示轻量“附件资料 · N 个文件”管理条；
- 创建前摘要中存在附件时提供“查看附件”。

所有入口打开同一个附件库。

## 3. 单一数据源

附件库统一读取：

- `directoryFiles`
- `taskFiles`
- `routedAttachmentFiles`
- `sharedFiles`

用户在任一阶段增删附件后会立即：

1. 重建附件索引；
2. 重新解析邮件附件引用；
3. 更新“已匹配 / 待匹配”；
4. 更新附件待办数量；
5. 更新选择与安排、创建前摘要。

从导入阶段自动识别出的附件如果被用户明确移除，会记录忽略身份，避免下一次路由重算时悄悄重新出现。

## 4. Dialog 视觉重构

旧结构：整个 Dialog `overflow:auto`。

新结构：

- Header 固定；
- Body (`nmda-supplement-grid`) 独立滚动；
- Footer 固定；
- Dialog 外层 `overflow:hidden` 并独立承载完整 shadow。

这样解决底部阴影看似被切断、Footer 与内容层粘连、滚动时边界不清晰的问题。

同时 `.nmda-btn` 统一为 `inline-flex + align-items:center + justify-content:center`，因此 `<label>` 文件选择按钮和原生 `<button>` 在相同高度下具有一致的文字垂直中心。

## 5. 信息层级

附件管理遵循以下层级：

- **当前缺附件**：一级待办；
- **附件已匹配**：轻量持续资产状态；
- **文件明细**：按需查看；
- **底层匹配算法**：不暴露给用户。

“优先级降低”不会再导致“可见性消失”。

## 6. 验证

本轮验证包括：

- 19/19 Node 回归测试通过；
- 新增 Attachment Library contract；
- Chromium 实测：复杂批次导入 3 个附件后，弹窗立即显示完整附件列表；
- 单个删除后数量从 3 → 2，重新加入后恢复 3；
- 完成准备后从上传页重新打开同一附件库，3 个文件保持一致；
- Task Hierarchy Playwright 测试通过；
- 完整复杂旅程（导入 → 待办 → 去重 → 附件 → 排期 → 创建前）通过；
- 页面级 JavaScript error = 0。
