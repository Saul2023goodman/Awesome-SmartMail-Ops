# v1.47 · LEAN REVIEW

## 目标

上传后的核验步骤只做三件事：找到文件、查看真实内容、确认用途。

## 调整

- 删除文件列表中的“快速核验”中间列，避免与右侧内容预览重复。
- 文件列表只保留文件身份与用途，释放横向空间给长文件名和目录。
- 参考总名单、附件文件、附件目录改为左侧常驻上传入口，不再默认折叠。
- 去除补充资料中的“跳过/暂不添加”按钮；没有资料时直接继续即可。
- 真实内容继续集中在右侧检查器，分类修改仍只有一个主入口。

## 设计原则

列表用于定位，右侧用于判断，上传入口始终可见。每种信息只出现一次。
