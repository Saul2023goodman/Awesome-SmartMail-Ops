# v1.18 业务状态重构

## 1. 根本问题

旧逻辑把“待处理”部分当成了历史状态：一封邮件因为缺主题进入队列后，即使主题已经补齐，修改动作仍会写入 `reviewDraftPending`，于是邮件继续显示为“待确认”。这会让用户做一次没有新增信息的确认。

同一问题也出现在附件：系统已经知道“资料要求 CV.pdf，但用户没有上传”，却把它和其他运行错误一起放进通用错误数组，因此界面只能显示“其他问题”，没有告诉用户下一步应该去添加附件。

## 2. 新状态模型

状态改为由“当前未解决事项”实时推导：

| 类别 | 例子 | 处理位置 | 修复后 |
| --- | --- | --- | --- |
| 内容待补 | 缺收件人、邮箱无效、缺主题、缺正文 | 检查邮件 | 自动移出待处理 |
| 需要核对 | 低置信度、解析边界不清、名单冲突、人工改写完整邮件 | 检查邮件 | 用户确认后通过 |
| 附件待加 | 资料要求文件但本地未提供 / 同名冲突 | 添加资料 | 匹配成功后自动解除 |
| 时间提示 | 导入时间无法识别 / 已过期 | 选择与安排 | 在排程区重新选择，不阻塞资料阶段 |
| 停止联系 | 联系人策略为“不再联系” | 自动处理 | 只停该封，不阻塞整批 |
| 其他阻塞 | 无法归入以上类别的真实错误 | 对应上下文 | 保留为兜底，不作为常规分类 |

## 3. 一键主题的新行为

当用户填写一封原本缺主题的邮件时：

1. 当前邮件立即重新计算问题。
2. 如果它只有“缺主题”这一项，立即变为可直接使用，不再要求确认。
3. 若同批还有空白主题，系统就地询问是否使用同一主题。
4. 用户点击一键填写后，只修改空白主题，不覆盖已有主题。
5. 每一封被修改的邮件独立重新计算状态：
   - 只缺主题 → 自动通过；
   - 仍缺收件人/正文 → 继续待处理；
   - 有解析歧义/名单冲突 → 继续等待人工确认。
6. 如果当前处于“待处理”筛选，已经解决的邮件立即从队列移除，并自动定位到下一封仍需处理的邮件。

## 4. 附件的新行为

当来源资料中出现附件引用时：

- 解析结果直接显示“附件待加 N”。
- 首次发现缺附件时，附件区域自动展开。
- 文案直接说明“资料要求 X 个附件；已找到 Y 个；还有 N 个尚未提供”。
- 缺失列表直接显示文件名，并提供对应文件选择。
- 用户选择文件/文件夹后自动重建索引并匹配。
- 匹配成功后相关任务自动恢复，不需要再回邮件检查区确认。
- 右侧流程的“添加资料”步骤在附件未齐时保持当前状态，并显示“还需添加 N 个附件”。

## 5. 同类业务问题的处理原则

### 导入时间无法识别

旧逻辑：当前步骤报错并阻塞，但真正的排程功能在下一步，形成死路。

新逻辑：把原时间视为不可复用的信息，给出提示，并允许进入默认展开的自动排程重新选择。

### 联系限制

旧逻辑容易被理解为“整批有错误”。

新逻辑：“不再联系”属于任务策略，不属于资料错误。对应邮件自动禁用，其他邮件继续。

### 名单冲突

学校/联系人冲突不是确定性字段缺失，系统不能自行判断哪一个正确，因此继续进入“需要核对”，明确交给用户确认。

## 6. 状态机原则

**问题出现 → 路由到能解决它的位置 → 用户/系统修复 → 重新计算事实 → 自动解除已解决状态。**

不再使用“因为它曾经有问题，所以继续要求确认”的历史式状态。
