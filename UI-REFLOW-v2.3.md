# UI Reflow v2.3 · Adaptive Decision Canvas

## 这次建议反映出的核心问题

本轮反馈并不是“再缩一点尺寸”能够解决的问题，而是三个布局原则没有真正贯彻：

1. **禁止外层滚动 ≠ 禁止内容滚动。**
   旧版在移除页面滚动后，部分工作面仍采用固定轨道和 `overflow:hidden`。当窗口高度下降时，内容没有新的去处，只能被裁切。
2. **响应式不能只看宽度。**
   1366×611 这类“宽但矮”的窗口不是窄屏，却比 820×700 更容易出现纵向不足。旧规则主要由 `max-width` 驱动，因此会错误保留桌面双栏/固定高度结构。
3. **固定 UI 必须按持续价值付费。**
   左侧 200px 导航、64–68px 顶栏、额外页面标题栏、较高流程条长期占据视口，但用户每一步真正操作的是中间工作区。固定区越大，业务区越被迫压缩。

因此 v2.3 的目标不是“让所有东西硬塞进一屏”，而是：

> 固定区域尽可能小；每个任务只占一个画布；同一任务内部再按决策拆分；只有长列表或极端短窗口下的当前任务内容允许滚动。

## 新布局契约

### 1. 全局框架

- 顶栏由 52px 降为 52px 基准、短窗口 48px。
- 左侧工作区导航由 208px 收缩为 70px；窄窗口 64/58px。
- 删除独立页面标题栏，当前上下文由左侧模块、步骤条与任务卡标题共同表达。
- 移除顶部“不会自动发送/不自动发送”徽章等重复安全说明。
- 邮箱状态仅保留连接状态、账号和“连接邮箱/切换邮箱”动作。

### 2. 批量步骤条

步骤条从较高的说明卡改为 42px 进度切换器：

`导入资料 → 核验待办 → 选择与排期 → 创建草稿`

步骤说明小字不再常驻。窄窗口仍保留步骤名称，不退化为无法识别的纯数字。

### 3. 排期

“选择与排期”不再尝试把规则与邮件列表同时显示。所有窗口统一为两个子画布：

- **排期规则**：只显示开始时间、同校每轮上限、同校间隔、保留已有时间、避开节假日/周末和应用动作。
- **邮件时间**：只显示筛选/排除与邮件时间列表。

规则画布默认不滚动；只有当前窗口高度确实不足时，规则卡本身成为唯一纵向滚动拥有者。页面、步骤外壳和预览卡不滚动。

### 4. 高度响应式

新增 `max-height` 适配而不只依赖 `max-width`：

- 在高度 ≤ 680px 时优先压缩顶栏、步骤条、卡片边距和输入框高度。
- 1366×611 被加入正式自动截图基线。
- 邮件时间视图使用 `max-content + minmax(0,1fr)` 分配轨道，筛选区只占自身高度，剩余空间属于邮件列表，不再产生不可用的大块空白。

## 滚动所有权

允许滚动：

- 文件列表 / 文件详情
- 附件列表
- 邮件时间表格
- 联系人表格
- 极端短窗口下的“排期规则”当前任务画布

禁止滚动：

- 页面 `.nmda-page`
- 批量步骤外壳
- `#nmda-preview-card`
- 弹窗遮罩
- 同一支路的嵌套纵向滚动

## 文案清理

主界面移除：

- “不自动发送”顶部徽章
- “只创建 / 保存草稿，不自动发送”创建前提示
- 单封/批量完成后的“不会自动发送”重复尾句
- 规则区的重复技术解释

保留真正影响决策的文字，例如“保留已有时间”“避开节假日和周末”。

## QA

自动 UI QA 现在覆盖：

- 1440×900
- 1280×800
- 1100×760
- 920×720
- 820×700
- **1366×611 short-desktop**

并单独截图排期规则与邮件时间的 1366×611 状态。当前自动布局检测：**P0 = 0，P1 = 0**。
