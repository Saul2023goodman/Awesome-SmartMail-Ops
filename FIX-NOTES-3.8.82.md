# v3.8.82 — 全量 SmartMail 数据重置

- 在全局头部新增“清空全部”入口，不再需要在选择/排期阶段逐封排除任务。
- 新增二次确认对话框，明确区分 SmartMail 本地数据与 163 邮箱真实邮件。
- 清空范围：当前导入、全部任务、Review 状态、附件映射、排期、Follow-up 本地状态与模板/规则、邮箱历史读取范围、SmartMail 本地工作区缓存。
- 不删除 163 中的已发送、收件、草稿或定时邮件。
- 仅移除 SmartMail 自有 localStorage key，不调用 localStorage.clear()，避免影响 mail.163.com 站点数据。
- 清空时等待正在进行的邮箱读取结束后再丢弃结果，避免异步结果回填到已重置的状态。
- 批量执行草稿期间禁止全量重置，必须先停止执行。
