# v1.35 · Roster & Layout Hardening

## 这次解决的根因

- Excel 纵向合并单元格只在首行保存院校值，旧解析器把后续导师视为院校为空。
- 总名单匹配只依赖首个邮箱或单一姓名字符串；多收件人、姓名顺序、显示名和重复行会造成误判。
- 排程即使取得院校，也优先按邮箱域名分组，同校多域名可能被拆开。
- 主页面、任务表、导入弹窗和宿主页面同时拥有纵向滚动，造成多滚轮、遮挡与焦点迷失。
- 创建操作位于长表格末端并再次吸底，切断列表且与右侧流程导航重复。

## 系统性修复

1. XLSX 解析阶段展开纵向合并单元格；总名单解析阶段再对层级空白做安全继承，并在空白分隔行重置上下文。
2. 联系人匹配改为多证据评分：所有收件邮箱、显示名、识别标题、任务名、姓名顺序变体、院校和邮箱域名共同参与；精确邮箱始终优先。
3. 重复名单行若身份一致则选取信息更完整的一行；院校或身份真正冲突时才进入人工确认。
4. 排程优先使用名单/人工院校作为业务分组，邮箱域名只作回退，并按实际回退邮件数反馈。
5. 右侧流程栏新增独立创建卡，创建按钮、选择摘要、附件与定时状态始终在同一视觉上下文中。
6. 批量任务表取消内部纵向滚动；打开工作台时锁定宿主页面滚动；弹窗由遮罩层单独承担滚动，正文不再产生第二滚轮。

## 回归边界

- 总名单仍是可选增强；未上传时不阻断当前批次查重和创建流程。
- 不凭低置信度简称强行合并院校；证据冲突继续保守提示。
- 创建行为仍只保存草稿，不自动发送。
