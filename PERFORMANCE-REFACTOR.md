# v1.16 性能重构说明

## 卡顿的主要原因

### 1. 页面切换等于全量刷新

旧实现切到批量草稿后会同时重画任务表、标签、排程、附件、解析概况、总名单核对和流程状态；切到联系人会重新排序并生成大量联系人行。切换动作因此承担了与数据规模相关的计算和 DOM 创建。

新版把“页面是否可见”和“数据是否需要刷新”分开：数据变化只标记 dirty；页面可见时再在下一帧合并刷新。单纯切换页面不会重算没有变化的数据。

### 2. 小操作触发大刷新

旧实现搜索每输入一个字符就重建列表，单封草稿正文每个输入事件都会写浏览器存储，联系人改一个状态还会连带刷新批量视图。

新版分别采用短防抖、延迟保存和局部失效。只有真正影响任务结构的变化才重建任务。

### 3. 大列表一次性创建 DOM

联系人可一次生成最多上千行，解析预览也可能一次创建大量条目；每次重建还要重新绑定行级监听器。

新版联系人和解析队列每次显示 250 条，用户需要时继续加载；批量表和联系人表使用容器级事件委托，避免随行数增长的监听器创建与销毁。

### 4. 总名单核对重复扫描

旧实现每一封邮件都重新扫描整份总名单，数据量增大后接近“邮件数 × 名单人数”的工作量。

新版先为名单建立邮箱、姓名和唯一键索引，之后每封邮件通过索引查找。合成 3,000 封邮件 × 3,000 人的本地测试中，核心核对从约 139 ms 降到约 33 ms；这是算法核心测试，不代表完整浏览器端到端耗时。

### 5. 固定模糊层增加滚动绘制

多个 sticky 区域同时使用背景模糊，会增加滚动时的合成与绘制工作。新版保留布局层级，但使用实色/高不透明背景，并对工作区和离屏卡片增加 containment / content-visibility。

## 性能策略

- **Dirty state**：只有数据变化才重新计算。
- **Lazy render**：隐藏页面不构建大列表。
- **Frame coalescing**：同一帧内多个更新合并。
- **Debounce**：搜索、筛选和输入保存不跟随每个按键执行。
- **Event delegation**：大表格只保留容器级事件监听。
- **Progressive rendering**：联系人和解析队列分段显示。
- **Indexed lookup**：名单核对从重复全表扫描改成索引匹配。
- **Rendering containment**：降低插件工作台对网易邮箱页面其余区域的布局/绘制影响。
- **Single-pass evidence scan**：附件上传证据每轮只扫描一次 Compose DOM，不再按附件数量重复扫描。
- **Throttled compatibility scan**：定时草稿成功页的宽范围兼容扫描降低频率，正常轮询优先检查语义节点。

## 验证

已执行：

- 所有 JavaScript 语法检查；
- 批量补主题回归；
- 解析预览批量保存回归；
- UI 结构契约检查；
- 性能结构契约检查；
- 名单索引匹配的合成数据正确性与耗时测试。

真实网易邮箱中最终体感仍取决于浏览器、邮件数量、联系人数量和网易页面本身，但新版已移除插件自身最明显的全量重算与大 DOM 热点。
