# UI Reflow v2.7 · Direct Repair + Duplicate Stack

## 本轮业务判断

邮件审阅不是统一的“人工处理页”。自动识别完成后，异常应按性质分流：

1. **确定性缺失**（收件人 / 主题 / 正文为空或无效）：系统已经知道哪里错，不需要用户再点击“发现问题，修正”。打开邮件后直接显示缺失字段输入区，同时保留完整邮件审阅上下文。
2. **不确定性问题**（称呼、落款、边界、低置信度）：默认保持只读完整审阅，重点看开头、结尾与邮件边界；只有用户发现错误才进入全量修正。
3. **重复 / 冲突**：不是单封邮件错误，而是一个“版本组决策”。主界面只显示一张堆叠组卡，点击后同时比较组内版本并决定保留哪些。

## 交互规则

- 缺失项点击卡片后立即可输入，不增加二次按钮。
- 缺失项修复完成后重新跑当前事实校验；若仍有软性疑点，则回到完整审阅，不允许借修复动作跳过人工判断。
- 重复组在主界面只占一个决策单元，避免同一问题重复占卡片。
- 重复卡使用“叠纸 + 版本行 + 推荐版本”表达多个候选，但所有视觉层都被限制在卡片自身 paint containment 内，不侵入相邻网格。
- 重复组只提供“比较并取舍”入口，不提供危险的一键删除默认动作。
