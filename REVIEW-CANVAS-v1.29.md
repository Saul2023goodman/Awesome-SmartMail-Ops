# v1.29 · Review Canvas 重构

## 1. 真实用户在当前页面要完成什么

当批次已经进入“选择与安排”或只剩少量人工核对时，用户的核心动作不是继续理解解析器，而是：

1. 在几十封邮件中快速定位目标；
2. 核对收件人、主题、正文；
3. 必要时修改；
4. 顺序查看上一封 / 下一封；
5. 极少数情况下从本批次排除一封；
6. 查看完后返回“选择与安排”。

因此页面应该围绕 **列表导航 + 邮件正文 + 顺序切换** 建立视觉中心。危险操作、识别证据和批量确认都属于次级能力。

## 2. 原页面暴露的问题

### 2.1 “排除此封”被错误提升成长期主操作

旧版把红色排除按钮放在横跨编辑器的 sticky 操作条中。它会：

- 直接覆盖邮件正文；
- 在滚动时持续占据视野；
- 让低频、可恢复的破坏性动作比“阅读邮件”更显眼；
- 破坏正文阅读连续性；
- 与右侧流程、顶部状态同时竞争视觉注意力。

这不是单纯的样式问题，而是操作层级错误。

### 2.2 页面上下文重复

旧版同时存在“批量草稿”“处理待办”“当前：…”“邮件内容”“流程 3/4”等多层标题。它们分别来自不同开发模块，但对用户而言表达的是同一件事：我正在查看这一批邮件。

重复层级消耗了最宝贵的垂直空间。

### 2.3 53 封邮件只能依赖滚动定位

长列表下，用户主要靠滚动条位置和编号找邮件：

- 很难快速跳到某位联系人；
- 点开一封后再回列表容易失去位置；
- 连续抽查需要不断鼠标移动和滚动。

### 2.4 “处理待办”与真实步骤不一致

当待办已经清零、右侧流程处于 3/4“选择与安排”时，中央仍显示“处理待办”，造成状态语义冲突。

### 2.5 为了防止按钮丢失而使用 sticky，反而制造遮挡

“关键按钮不要滚丢”是正确目标，但不能简单推导成“所有按钮都 sticky”。正确做法应区分：

- 高频、当前必须完成的确认：可以成为编辑器正常 footer；
- 低频、危险、可恢复动作：进入 contextual overflow menu；
- 页面最终 CTA：可 sticky，但必须位于自己业务阶段的容器边界，而不是覆盖内容。

## 3. v1.29 的页面模型

Review Canvas 由三部分构成：

### 左侧：邮件导航 rail

- 邮件列表；
- 姓名 / 邮箱 / 主题搜索；
- 有待办时才显示“只看待办 / 全部邮件”；
- 只有待办模式才显示批量确认；
- 列表独立滚动，不带走正文上下文；
- 当前邮件保持高亮。

### 中间：邮件编辑 canvas

顶部只保留：

- 当前邮件名称；
- 当前状态 / 待办；
- 上一封；
- `当前 / 总数`；
- 下一封；
- “更多”。

主体只有：

- 收件人；
- 主题；
- 正文；
- 按需展开的识别依据。

### 右侧：全局流程 rail

继续只负责回答：

> 我在整个批量流程的哪一步？

它不参与单封邮件操作。

## 4. 关键交互调整

### 4.1 排除动作移入“更多”

默认页面不再显示红色排除按钮。

只有用户主动打开：

`更多 → 从本批次排除`

才显示危险操作，并明确说明：

> 不会创建这封草稿；之后仍可恢复。

由于排除是可恢复操作，不增加二次确认弹窗，避免制造不必要步骤。

### 4.2 显式确认成为正常 footer

当某封邮件确实需要人工明确确认时，编辑器底部显示：

- 确认本封；
- 确认并下一封。

该区域是编辑器 grid 的第三行，不使用 overlay，不覆盖正文。

邮件不需要确认时，整个 footer 不占空间。

### 4.3 进入 Review Canvas 时隐藏重复页标题

“批量草稿”作为工作区导航已经存在于左栏。

进入专注审阅后，再重复展示大号“批量草稿”页标题没有帮助，因此自动隐藏，把空间交还给邮件。

离开 Review Canvas 后标题恢复。

### 4.4 根据业务状态自动改名

有 blocker：

> 处理待办

待办清零：

> 查看邮件

若已经进入选择阶段，退出按钮变成：

> 返回选择与安排

而不是技术性的“关闭 / 收起预览”。

### 4.5 长列表增加搜索和顺序导航

新增：

- `搜索姓名 / 邮箱 / 主题`；
- 上一封；
- `1 / 53`；
- 下一封。

搜索后导航只在搜索结果内移动；清空搜索后恢复整批邮件。

## 5. 设计原则

本轮吸收并落地了几类成熟设计系统常用原则：

- **Contextual actions**：低频危险动作不要长期占据主视野；
- **Progressive disclosure**：识别依据、危险操作按需出现；
- **Stable canvas**：用户阅读/编辑的核心对象不应该被浮层盖住；
- **Master-detail**：长列表负责导航，右侧/中间 detail 负责处理当前对象；
- **One surface, one hierarchy**：同一个页面不要重复多个“当前任务”标题；
- **Independent scrolling**：长队列和邮件正文分别滚动，避免整个页面位置漂移；
- **State-derived copy**：文案由当前业务状态决定，而不是由组件历史名称决定。

## 6. 1366×612 / 53 封运行验证

VM 使用 53 封完整邮件模拟用户截图中的长批次，并在待办清零后重新进入邮件抽查。

运行结果：

- 当前流程：`流程 3 / 4`；
- 工作区标题：`查看邮件`；
- 邮件导航：`1 / 53` → 点击下一封 → `2 / 53`；
- 搜索 `Candidate 25` 后只剩 1 个结果，并正确打开 Candidate 25；
- 左侧队列可视高度：365 px；
- 左侧队列内容高度：3190 px，确认独立滚动有效；
- 正文 textarea 在 1366×612 下保持 210 px 可见高度；
- 持久悬浮操作条：不存在；
- 重复“批量草稿”页标题：Review Canvas 中隐藏；
- 页面级 JavaScript error：0。

运行数据保存在 `vm/review-canvas-metrics.json`。

## 7. 回归约束

后续迭代应继续遵守：

1. 不允许任何持久操作条覆盖正文；
2. 排除 / 删除等低频危险动作默认不得成为页面主按钮；
3. 进入邮件抽查后必须保持列表搜索能力；
4. 当前邮件必须有明确的位置反馈；
5. 0 待办时不再显示“处理待办”；
6. 已进入步骤 3 后，抽查邮件不能把流程视觉错误切回步骤 2；
7. 技术证据保持二级 disclosure；
8. 长列表必须由列表自己滚动，不能依赖整个插件页面滚动。
