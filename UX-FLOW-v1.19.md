# v1.19 自动推进与编辑优先

## 1. 本轮修正的核心逻辑

工作台不再把“确认”当成每封邮件都必须经过的动作，而是按信息价值区分：

- **确定性缺失**：缺收件人、缺主题、缺正文。用户补齐后即可由系统验证，自动完成。
- **真正需要判断**：解析边界不清、名单冲突、用户主动改写一封原本完整的邮件。只有这些情况保留人工确认。

因此，主题一键填写后，如果对应邮件已经没有其它未解决事项，会直接从待处理队列消失；不会再要求“全选 → 确认所选”。

## 2. 当前事实替代历史状态

识别阶段的置信度会受到缺字段影响。例如没有 Subject 标记时，原始识别分数天然更低。用户后来手工补上主题后，不能继续拿旧分数作为阻塞理由。

v1.19 会依据当前字段重新计算有效识别证据：

- 原来缺主题，当前主题已补齐 → 恢复主题证据；
- 原来缺收件人，当前邮箱有效 → 恢复收件人证据；
- 原来正文过短，当前正文已经完整 → 恢复正文证据。

只有仍然存在与缺字段无关的歧义，才继续要求人工判断。

## 3. 完成后自动去下一件事

每次问题解决后，不再把用户留在原地。

流程按以下优先级自动路由：

1. 还有其它待处理邮件 → 自动打开下一封；
2. 邮件都处理完，但还有缺附件 → 自动收起邮件检查并定位到附件区；
3. 还有其它真实阻塞 → 回到对应待办汇总；
4. 所有阻塞清零 → 自动进入“选择与安排”，并将自动排程保持展开。

即使最后一封是需要人工判断的邮件，点击“确认本封”后，如果已经没有其它待办，也会直接进入下一阶段。

## 4. 邮件检查改为编辑优先

用户真正要确认和修改的是“最终写入网易草稿的邮件”，因此视觉优先级调整为：

**邮件列表 → 可编辑邮件内容 → 解析依据（按需）**

邮件内容位于主区域顶部，正文编辑区增高。原文不再与编辑器争夺主视图，而是收进“解析依据”：

- 原始文件/内容来源；
- 收件人、主题、正文、边界识别步骤；
- 当前有效识别比例；
- 邮箱候选；
- 邮件起止块、Subject、称呼、落款等原文标记。

当系统判断存在边界、邮箱或名单歧义时，“解析依据”会自动展开；普通缺主题等确定性问题默认不展开。

## 5. 批量确认降级

“选择当前列表 / 确认所选”不再对所有待处理邮件出现。

只有同时满足以下条件的邮件才可进入批量确认：

- 收件人、主题、正文都已完整；
- 仍存在真正需要人工确认的歧义。

因此，确定性缺失不会通过“批量确认”绕过填写，也不会在填写完成后再制造第二次确认。

## 6. 设计原则

**系统能确定，就自动完成；需要用户判断，才请求确认；一个阶段完成，就直接把用户带到下一个实际动作。**
