# v1.45 · Source-Isolated Review

## 修复目标

上传后核验必须满足一个基础条件：**用户点到哪个文件，就只看到哪个文件自己的内容。**

此前多个 Word 邮件被合并为一个执行集合后，分类核验界面也直接使用了该集合做预览，导致不同文件可能重复显示第一封邮件的收件人、主题或正文。

## 数据结构调整

- 多 Word 邮件仍保留一个合并执行集合，保证后续批量任务生成效率。
- 每个原始 Word 同时保留独立的 source shadow，专门负责单文件预览、分类和人工修正。
- 合并集合不再作为单文件预览来源。
- 每条合并任务保留其原始来源路径，右侧预览和列表摘要按来源文件精确匹配。
- 单独把某一 Word 改成附件 / 总名单 / 暂不使用时，只影响该文件；合并执行集合在生成任务时自动跳过它。
- 文件夹导入保留完整相对路径，减少同名文件在不同目录下互相串联的风险。

## UI 调整

- 文件格式和业务用途分成两套视觉语言：
  - 文件格式：DOCX / XLSX / PDF / EML / ZIP 等独立图标色。
  - 业务用途：邮件 / 总名单 / 附件 / 待确认 / 暂不使用使用独立色条和用途徽标。
- 文件图标放大，并直接写出 DOCX、XLSX、PDF 等格式缩写。
- 用途标签加强颜色和图形差异，不再只靠浅色小圆点。
- 选中行保留当前业务用途的颜色，不再所有文件都统一蓝色。
- 右侧去掉重复的“文件核验”标题，从文件本身直接开始阅读。

## 回归

新增 source-isolation 回归用例，验证合并 Word 后仍保留两个独立 source shadow。
