# SmartMail Ops v3.8.47 — 名单分批 UI 体系重构

## 目标
把名单分批从“三栏解释型界面”改成“画布优先、选择即核验”的操作界面。用户只需要关注两件事：选中联系人、设定批次。

## 核心变化

- 移除常驻左侧颜色栏和右侧批次面板，Excel 画布获得完整主空间。
- 总名单来源、人数统计、颜色快捷选择、列显示控制收敛到顶部紧凑工具条。
- “当前分批”改为可交互的批次分布带：R1 / R2 / R3 / … / 待分按人数可视化显示。
- 点击任一批次，直接在 Excel 中选中并高亮该批次成员；点击“待分”可立即看到还没分批的人。
- Excel 行号旁直接显示当前批次徽标（R1、R2…），无需额外对照表。
- 框选、颜色选择、批次核验三种选择方式统一进入同一套就地操作条。
- R1–R4、更多批次、移出批次全部放在 Excel 上方，不再遮挡右侧内容。
- 保留 v3.8.45 的合并单元格还原、必要列聚焦与原 Excel 样式。
- 保留 v3.8.46 的颜色选择与自由框选能力，以及既有排期/附件/邮箱执行逻辑。

## 用户路径

1. 打开“名单分批”。
2. 通过框选、颜色、或点击已有批次选中联系人。
3. 直接点击 R1 / R2 / R3 / R4 或更多批次。
4. 结果立即显示在行号批次徽标和顶部批次分布带中。
5. 点击任一批次即可反向核验对应成员。
6. 完成后进入“时间规则”。

## 验证

- `app.js`、`background.js`、`executor.js`、`roster-planner.js`、`scheduler.js` 均通过 `node --check`。
- `manifest.json` JSON 校验通过。
- 版本更新为 `3.8.47`。
