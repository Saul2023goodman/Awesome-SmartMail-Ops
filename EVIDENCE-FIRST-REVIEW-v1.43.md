# v1.43 · Evidence-first Review

## 这一步真正要完成什么

上传后的核验不是“管理分类规则”，而是让用户用最小成本回答两个问题：

1. 系统把这个文件当成什么？
2. 看过文件内容后，这个判断对不对？

所以 UI 的空间优先级应当是 **文件证据 > 当前结果 > 修正动作 > 技术诊断**。

## 本版收敛

### 1. 一个主要修正入口

- 删除文件行内用途下拉框。
- 删除行尾“查看”箭头；点击整行即查看。
- 删除右侧五枚并列分类按钮。
- 右侧只保留一个“文件用途”选择器。

这样“修改用途”只有一个显式入口，不再让用户在三套交互中选择。

### 2. 文件信息前置

列表每行直接显示：

- 文件名；
- Word / PDF / 表格等文件类型；
- 原目录；
- 文件大小；
- 用户可理解的判断说明；
- 内容摘要；
- 当前用途。

多数文件无需打开右侧即可快速抽查。

### 3. 右侧从操作面板变成证据面板

- 邮件：收件人、主题、正文摘要；
- 总名单：表格前几行；
- 待确认：仍展示文件内容，而不是只告诉用户“需要确认”；
- 附件 / 暂不使用：尽可能展示读取到的正文或表格内容。

只有内容读取确实错误时，才展开“进一步调整”。

### 4. 拖拽按需出现

拖拽仍然适合批量纠错，但不值得长期占据一整排空间。

- 平时隐藏五类拖拽目标；
- 用户按住拖拽手柄后，目标分类托盘浮出；
- 松手完成归类后自动消失。

### 5. 去除重复筛选

顶部五类统计已经支持点击筛选，因此删除独立“只看待确认”按钮。点击“待确认”统计即可进入异常文件视图。
