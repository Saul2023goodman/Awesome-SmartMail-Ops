# v1.31 Context Preflight：把“有用上下文”前置到最能减少返工的时点

## 目标

v1.31 不把“参考总名单”设计成一个隐藏在“更多资料”里的附加功能，也不把它错误升级为强制依赖，而是把它变成导入阶段的**可选上下文预检**：用户导入邮件后，系统主动说明上传总名单能带来什么收益，并要求用户做一次明确选择——**现在上传**或**本批次暂不添加**。

核心产品原则：

> **凡是某份上下文能显著改变后续判断，就应该在“第一次还能低成本补充”的时点主动提示；但可选上下文不应伪装成硬性前置条件。**

这比“出错后再提醒”更用户导向，因为它把系统从错误反馈器变成返工预防器。

---

## 1. 为什么参考总名单应该在导入阶段被主动提示

参考总名单对后续至少有四类提升作用：

1. **更早发现历史/名单维度的重复与遗漏**
   - 当前批次本身仍会独立查重；
   - 上传参考总名单后，还能增加“是否已在既有名单中、是否名单外、是否遗漏”的交叉核验上下文。

2. **提高联系人归属判断的稳定性**
   - 当邮件只有姓名、简称、院校或多个邮箱候选时，总名单可以提供额外身份上下文；
   - 这类信息越晚加入，越容易让用户先完成一轮人工修正后又返工。

3. **提前补充排期所需的院校上下文**
   - 院校不是用户必须维护的邮件字段，但可用于同校错峰；
   - 总名单如果能补足院校信息，排期阶段就不需要临时再解释或人工补数据。

4. **让后续数字基于真实业务状态重算**
   - 重复、排除、院校分组和名单差异都可能因总名单加入而改变；
   - 因此最合适的时点是“邮件已经导入、但用户尚未开始处理待办”这一窗口。

如果把它放到步骤 2 或步骤 3 才提示，用户很可能已经完成了一部分人工检查；如果完全隐藏在“更多资料”，很多用户根本不会知道它为什么值得上传。

---

## 2. v1.31 的导入交互

### 导入前

界面轻量提示：

- “有参考总名单？建议与邮件资料一起加入”
- 解释作用，但不抢占“添加邮件资料”的主入口。

### 邮件导入后、尚无总名单

步骤 1 保持为当前步骤，出现明确上下文卡：

**邮件已导入，是否补充参考总名单？**

主要操作：

- **现在上传总名单**
- **本批次暂不添加**

用户必须做一次明确选择，但两条路径都能继续。

### 已上传

提示变为紧凑状态：

- “参考总名单已加入（N 条）”
- 可以补充/替换或移除
- 不再长期占据视觉中心

### 明确跳过

显示：

- “暂不使用参考总名单”
- 当前批次基础查重继续工作
- 后续仍可补充

这避免了两种错误设计：

- **隐藏**：用户不知道有这个能力；
- **强制**：用户没有名单时被流程卡死。

---

## 3. 从“总名单提示”抽象出的通用规则

### 规则 A：Information before consequence

只有当一份信息会改变下一阶段决策时，才应该主动出现；而且应出现在决策发生**之前**。

### 规则 B：Optional ≠ invisible

“可选”不是“藏起来”。如果可选信息的收益很高，应主动解释收益，同时保留跳过权。

### 规则 C：显式跳过优于沉默缺失

系统需要区分：

- 用户尚未决定；
- 用户明确选择暂不提供。

否则流程无法知道是应该继续提醒，还是尊重用户选择。

### 规则 D：上下文一旦改变，依赖状态立即重算

新增/移除总名单、排除重复邮件、补附件等动作，都会改变后续待办数量和可执行状态。界面必须基于“当前仍存活的业务对象”重新计算，而不是保留历史提示。

### 规则 E：预检只问“用户需要决定的变量”

系统内部可以很复杂，但用户只需要看到：

- 现在缺什么上下文；
- 补了有什么收益；
- 跳过会发生什么；
- 下一步是什么。

---

## 4. 举一反三：推进到其它步骤

### Step 1：添加资料 —— 参考总名单预检

**时点：** 邮件资料已导入，尚未进入人工待办。

**提示内容：** 是否补参考总名单。

**收益：** 提前增强名单交叉核验、身份归属和院校上下文，减少中途返工。

**跳过后果：** 不影响批次基础查重与草稿流程，只失去额外交叉核验上下文。

---

### Step 2：处理待办 —— 附件上下文预检

**时点：** 系统已识别到邮件中存在附件要求。

**提示内容：** 建议现在一次性选择附件文件夹或文件。

**收益：** 系统可以提前批量匹配，而不是用户处理到每封邮件时才逐个寻找文件。

**跳过后果：** 可以先处理邮件内容；附件仍保留为待办，之后自动重新出现。

**设计变化：**

- 不让附件抢占比收件人/重复更高的优先级；
- 但提前让用户知道“如果你手边已有附件文件夹，现在加入能省一步”。

---

### Step 2 → 下一阶段：完成后的明确续接

**时点：** 邮件内容类待办已清零，但附件或下一阶段仍存在。

**提示内容：**

- “继续：添加附件（N）”；或
- “继续：选择与安排”。

**收益：** 用户无需退出当前页面后自己寻找下一模块。

这也是同一原则的延伸：**下一步本身也是一种上下文，应在用户最需要时直接出现。**

---

### Step 3：选择与安排 —— 排期上下文预检

**时点：** 用户已选好要创建的邮件，即将应用自动安排。

**只让用户确认两个真正影响结果的变量：**

1. 开始时间；
2. 同校间隔。

界面同时说明：

- 当前多少封已有院校信息；
- 如果有总名单，院校上下文已经加入；
- 未识别院校的邮件会保守错峰。

**收益：** 用户不需要理解分组算法、内部学校字段或 scheduler 细节，只确认实际会改变安排的参数。

---

### Step 4：创建草稿 —— 执行前结果预检

**时点：** 用户即将点击“创建 N 封草稿”。

**显示最小但足够的执行摘要：**

- 已选 N 封；
- 定时 N 封 / 普通草稿；
- 附件 N 份 / 无附件；
- 已排除 N 封（如有）；
- **只创建 / 保存草稿，不自动发送。**

**收益：** 用户在不可忽视的最后一步确认“将会发生什么”，而不是点击后再发现选择、定时或附件与预期不同。

---

## 5. 统一后的业务链路

新的主流程不再只是：

`添加资料 → 处理待办 → 选择与安排 → 创建草稿`

而是：

`添加资料`
→ **参考上下文预检**
→ `处理待办`
→ **附件上下文预检 / 动态续接**
→ `选择与安排`
→ **排期决策预检**
→ `创建草稿`
→ **执行结果预检**

这些预检不是新增四个流程步骤，而是嵌入原流程的“低成本决策点”。因此不会让流程变长，反而减少回退、查找、重新核验和误操作。

---

## 6. 后续功能应继续复用的判断标准

新增任何功能前先问四个问题：

1. **它会改变后续哪个决定？**
2. **用户最晚在什么时候提供它，仍然不会造成返工？**
3. **如果没有它，流程是否仍可以安全继续？**
4. **用户真正需要决定什么，哪些只是系统内部过程？**

如果答案是“可选但高收益”，就采用 v1.31 的模式：

**主动提示收益 → 明确上传/补充入口 → 提供清晰跳过 → 记录选择 → 在后续状态变化时重新计算。**

适合继续复用的场景包括：

- 历史发送记录导入；
- 多版本附件；
- 联系人名单补充；
- 主题模板/命名规则；
- 特殊排期约束；
- 创建前的批次安全检查。

但不应机械地为每个场景新增卡片。只有在该上下文**确实能减少返工或错误**时，才值得进入预检。

---

## 7. v1.31 验证结果

- 新增 Context Preflight contract 测试；
- 所有现有 Node 回归测试继续通过；
- VM clean-batch 验证：导入后总名单提示为 pending，步骤 1 保持 active；
- 明确跳过后可直接进入选择与安排；
- 上传 4 条参考总名单后状态正确变为 added；
- 排期前预检正确显示当前院校上下文；
- 创建前预检可实时从“普通草稿”更新到“定时 3 封”；
- 完整复杂旅程继续可从 8 项待办走到 5 封排期并保持最终创建入口可见；
- Chromium 页面级 JavaScript error：0。
