# v1.17 交互重构说明

## 1. 为什么“检查邮件”看不完整

旧版解析预览同时存在三层空间约束：

- 整个解析区被限制在约 `56vh`；
- 原文与邮件内容各自 `overflow:auto`；
- 正文 textarea 又只有约 145px 的最小高度并自行滚动。

因此长邮件虽然数据存在，但用户必须在多个小滚动区之间来回滚动，看起来像“内容显示不全”。

### 新结构

- 左侧 **邮件列表**：导航用途，可独立滚动并保持在视野内。
- 右侧 **原文 → 邮件内容**：按阅读顺序上下排列，不设置 viewport max-height。
- **正文**：根据 `scrollHeight` 自动扩展。
- **操作按钮**：回到正常文档流，不盖住正文。

这把滚动职责从“每个面板自己滚”收敛为“工作台主区域滚动”。

## 2. 为什么自动安排时间默认展开

它已经不是高级功能，而是“选择与安排”阶段的主要决策之一。用户进入这一阶段，本来就需要判断：

- 从什么时候开始；
- 同一学校一轮最多几位；
- 间隔多少天；
- 是否保留已给出的时间。

继续默认折叠只会增加一次无意义点击。因此每个新批次默认展开，但用户手动收起后不会被普通重绘强制打开。

## 3. 主题从“批量功能”变成“上下文建议”

旧交互要求用户：选中多封 → 找到批量主题输入 → 输入主题 → 应用。

新版把触发点放到真实动作之后：

1. 当前邮件原本没有主题；
2. 用户填完一个主题；
3. 系统发现同批还有其他空白主题；
4. 就地询问是否用同一主题补齐；
5. 用户明确点击后才写入，而且只写空白项。

这是一种“系统根据刚发生的操作提出下一步”的交互，而不是再增加一个长期功能入口。

## 4. 同一思路继续应用

- 切换下一封前先保留当前编辑，减少误丢内容。
- “确认”负责质量放行，“保留编辑”自动发生，两种语义拆开。
- 批量选择只显示批量确认，不再同时塞入主题编辑器。
- 关键内容默认展示；维护、映射、额外资料仍保持按需展开。

## 5. 安全边界

- 一键主题永远不覆盖已有主题。
- 只有用户点击一键填写才修改其他邮件。
- 自动保留编辑不会把邮件标记为“已确认”；确认仍需用户主动操作。
- 自动排程仍需用户点击“应用”才真正写入任务时间。
